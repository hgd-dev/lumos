diff -ruN '--exclude=dist' '--exclude=node_modules' '--exclude=.DS_Store' /mnt/data/lumos330_mobile_base_extracted/css/home-spiral.css /mnt/data/lumos330_smallmobile_work2/css/home-spiral.css
--- /mnt/data/lumos330_mobile_base_extracted/css/home-spiral.css	2026-07-30 20:23:36.477200800 +0000
+++ /mnt/data/lumos330_smallmobile_work2/css/home-spiral.css	2026-07-30 20:24:08.065199693 +0000
@@ -1574,6 +1574,150 @@
   .motion-plane-stack { right: -28%; width: 106vw; opacity: .42; }
 }
 
+
+@media (max-width: 430px) {
+  body.spiral-small-mobile-layout .spiral-item {
+    align-items: start;
+    padding: calc(66px + env(safe-area-inset-top)) 14px calc(104px + env(safe-area-inset-bottom));
+  }
+  body.spiral-small-mobile-layout .spiral-item-identity {
+    place-items: center;
+    padding: calc(72px + env(safe-area-inset-top)) 14px calc(92px + env(safe-area-inset-bottom));
+  }
+  body.spiral-small-mobile-layout .motion-identity-copy {
+    justify-items: center;
+    text-align: center;
+  }
+  body.spiral-small-mobile-layout .motion-scene-copy {
+    align-self: start;
+    width: min(100%, 92vw);
+    max-width: 348px;
+    padding-top: clamp(138px, 18vh, 188px);
+    text-shadow: 0 10px 26px rgba(2,3,4,.94), 0 2px 8px rgba(2,3,4,.98);
+  }
+  body.spiral-small-mobile-layout .motion-display-word {
+    font-size: clamp(2.7rem, 16.6vw, 4.25rem);
+    line-height: .82;
+    letter-spacing: .08em;
+  }
+  body.spiral-small-mobile-layout .spiral-item-identity .motion-display-word {
+    font-size: clamp(4.2rem, 22vw, 6.4rem);
+  }
+  body.spiral-small-mobile-layout .motion-typing-lockup-hero {
+    width: min(92vw, 520px);
+    font-size: clamp(1.3rem, 7.4vw, 1.95rem);
+    line-height: 1.01;
+  }
+  body.spiral-small-mobile-layout .motion-scene-index {
+    max-width: 100%;
+    font-size: .49rem;
+    line-height: 1.5;
+  }
+  body.spiral-small-mobile-layout .motion-scene-lead,
+  body.spiral-small-mobile-layout .motion-scene-summary,
+  body.spiral-small-mobile-layout .motion-scene-metrics dd {
+    max-width: 100%;
+    overflow-wrap: anywhere;
+    word-break: normal;
+  }
+  body.spiral-small-mobile-layout .motion-scene-lead {
+    margin-top: 11px;
+    font-size: clamp(.96rem, 4.9vw, 1.18rem);
+    line-height: 1.14;
+    text-wrap: balance;
+  }
+  body.spiral-small-mobile-layout .motion-scene-summary {
+    font-size: .72rem;
+    line-height: 1.48;
+  }
+  body.spiral-small-mobile-layout .motion-scene-actions {
+    display: grid;
+    grid-template-columns: 1fr;
+    width: min(100%, 300px);
+    gap: 10px;
+    margin-top: 14px;
+  }
+  body.spiral-small-mobile-layout .motion-scene-actions a {
+    justify-content: space-between;
+    width: 100%;
+    min-height: 42px;
+    padding: 11px 12px;
+  }
+  body.spiral-small-mobile-layout .motion-scene-metrics {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+    width: min(100%, 320px);
+    gap: 10px 12px;
+    margin-top: 16px;
+    padding-top: 12px;
+  }
+  body.spiral-small-mobile-layout .motion-scene-metrics div {
+    padding: 0 !important;
+  }
+  body.spiral-small-mobile-layout .motion-scene-metrics div + div {
+    padding-left: 0;
+    border-left: 0;
+  }
+  body.spiral-small-mobile-layout .motion-scene-metrics div:nth-child(3) {
+    grid-column: 1 / -1;
+  }
+  body.spiral-small-mobile-layout .motion-scene-metrics dt {
+    font-size: .44rem;
+  }
+  body.spiral-small-mobile-layout .motion-scene-metrics dd {
+    margin-top: 4px;
+    font-size: .63rem;
+    line-height: 1.34;
+  }
+  body.spiral-small-mobile-layout .motion-plane-stack {
+    position: absolute;
+    left: 50%;
+    right: auto;
+    top: 26%;
+    width: min(92vw, 390px);
+    opacity: .38;
+    justify-self: center;
+    transform: translate3d(calc(-50% + (var(--scene-plane-x, 0px) * .3)), calc(16px + (var(--scene-plane-y, 0px) * .3)), -100px) rotateY(-3.5deg) rotateX(1.35deg) scale(.66);
+  }
+  body.spiral-small-mobile-layout .plane-primary {
+    inset: 9% 5% 12% 0;
+  }
+  body.spiral-small-mobile-layout .plane-secondary {
+    right: 0;
+    bottom: 2%;
+    width: 41%;
+  }
+  body.spiral-small-mobile-layout .plane-tertiary {
+    top: 1%;
+    left: 5%;
+    width: 44%;
+  }
+  body.spiral-small-mobile-layout .spiral-stage-footer {
+    left: 12px;
+    right: 12px;
+    bottom: calc(12px + env(safe-area-inset-bottom));
+    gap: 10px;
+  }
+  body.spiral-small-mobile-layout .spiral-active-copy {
+    min-width: 0;
+  }
+  body.spiral-small-mobile-layout .spiral-active-copy strong {
+    max-width: 130px;
+    overflow: hidden;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+  }
+  body.spiral-small-mobile-layout .spiral-progress {
+    gap: 8px;
+  }
+  body.spiral-small-mobile-layout .spiral-progress button {
+    width: 26px;
+    height: 26px;
+  }
+  body.spiral-small-mobile-layout .spiral-scroll-cue {
+    display: none;
+  }
+}
+
 @media (max-height: 720px) and (min-width: 781px) {
   .spiral-item { padding-top:58px;padding-bottom:70px; }
   .motion-display-word { font-size:clamp(4rem,10vw,8.5rem); }
diff -ruN '--exclude=dist' '--exclude=node_modules' '--exclude=.DS_Store' /mnt/data/lumos330_mobile_base_extracted/js/home-spiral.js /mnt/data/lumos330_smallmobile_work2/js/home-spiral.js
--- /mnt/data/lumos330_mobile_base_extracted/js/home-spiral.js	2026-07-30 20:23:36.481200800 +0000
+++ /mnt/data/lumos330_smallmobile_work2/js/home-spiral.js	2026-07-30 20:24:08.065199693 +0000
@@ -16,6 +16,7 @@
 const reducedMotionQuery = window.matchMedia?.("(prefers-reduced-motion: reduce)");
 const coarsePointerQuery = window.matchMedia?.("(pointer: coarse)");
 const mobileLayoutQuery = window.matchMedia?.("(max-width: 780px)");
+const smallMobileLayoutQuery = window.matchMedia?.("(max-width: 430px)");
 const compactNavigationQuery = window.matchMedia?.("(max-width: 1080px)");
 const mobileMenuButton = document.querySelector("#homeMobileMenuButton");
 const primaryNavigation = document.querySelector("#homePrimaryNavigation");
@@ -50,6 +51,7 @@
 
 let activeCard = -1;
 let mobileLayout = mobileLayoutQuery?.matches === true;
+let smallMobileLayout = smallMobileLayoutQuery?.matches === true;
 let touchActive = false;
 let reduced = false;
 let stageIntersecting = true;
@@ -241,22 +243,23 @@
   }
 
   if (visible) {
-    const travel = Math.tanh(normalized * (mobileLayout ? 1.28 : 1.48));
-    const x = travel * viewportWidth * (mobileLayout ? -0.16 : -0.48);
-    const y = travel * viewportHeight * (mobileLayout ? -0.7 : -0.59);
-    const z = -absoluteNormalized * (mobileLayout ? 430 : 760);
-    const scale = clamp(1 - absoluteNormalized * (mobileLayout ? 0.08 : 0.12), mobileLayout ? 0.84 : 0.76, 1);
-    const rotateX = travel * (mobileLayout ? -2.2 : -4.2);
-    const rotateY = travel * (mobileLayout ? 4.8 : 9.5);
-    const rotateZ = travel * (mobileLayout ? -3.4 : -7.5);
+    const portraitMobileLayout = smallMobileLayout;
+    const travel = Math.tanh(normalized * (portraitMobileLayout ? 1.12 : mobileLayout ? 1.28 : 1.48));
+    const x = travel * viewportWidth * (portraitMobileLayout ? -0.06 : mobileLayout ? -0.16 : -0.48);
+    const y = travel * viewportHeight * (portraitMobileLayout ? -0.36 : mobileLayout ? -0.7 : -0.59);
+    const z = -absoluteNormalized * (portraitMobileLayout ? 320 : mobileLayout ? 430 : 760);
+    const scale = clamp(1 - absoluteNormalized * (portraitMobileLayout ? 0.05 : mobileLayout ? 0.08 : 0.12), portraitMobileLayout ? 0.9 : mobileLayout ? 0.84 : 0.76, 1);
+    const rotateX = travel * (portraitMobileLayout ? -1.2 : mobileLayout ? -2.2 : -4.2);
+    const rotateY = travel * (portraitMobileLayout ? 2.8 : mobileLayout ? 4.8 : 9.5);
+    const rotateZ = travel * (portraitMobileLayout ? -1.8 : mobileLayout ? -3.4 : -7.5);
     const cssText = [
       `transform:translate3d(${x.toFixed(1)}px,${y.toFixed(1)}px,${z.toFixed(1)}px) rotateX(${rotateX.toFixed(2)}deg) rotateY(${rotateY.toFixed(2)}deg) rotateZ(${rotateZ.toFixed(2)}deg) scale(${scale.toFixed(4)})`,
       `opacity:${opacity.toFixed(3)}`,
       "visibility:visible",
-      `--scene-copy-x:${(travel * viewportWidth * (mobileLayout ? 0.008 : 0.022)).toFixed(1)}px`,
-      `--scene-copy-y:${(travel * viewportHeight * (mobileLayout ? 0.01 : 0.015)).toFixed(1)}px`,
-      `--scene-plane-x:${(travel * viewportWidth * (mobileLayout ? -0.018 : -0.048)).toFixed(1)}px`,
-      `--scene-plane-y:${(travel * viewportHeight * (mobileLayout ? -0.018 : -0.03)).toFixed(1)}px`
+      `--scene-copy-x:${(travel * viewportWidth * (portraitMobileLayout ? 0.004 : mobileLayout ? 0.008 : 0.022)).toFixed(1)}px`,
+      `--scene-copy-y:${(travel * viewportHeight * (portraitMobileLayout ? 0.006 : mobileLayout ? 0.01 : 0.015)).toFixed(1)}px`,
+      `--scene-plane-x:${(travel * viewportWidth * (portraitMobileLayout ? -0.008 : mobileLayout ? -0.018 : -0.048)).toFixed(1)}px`,
+      `--scene-plane-y:${(travel * viewportHeight * (portraitMobileLayout ? -0.012 : mobileLayout ? -0.018 : -0.03)).toFixed(1)}px`
     ].join(";") + ";";
     setCardCss(index, cssText);
     renderedCardIndex = index;
@@ -573,9 +576,12 @@
 
 function updateResponsiveMode() {
   const nextMobileLayout = mobileLayoutQuery?.matches === true;
-  const changed = nextMobileLayout !== mobileLayout;
+  const nextSmallMobileLayout = smallMobileLayoutQuery?.matches === true;
+  const changed = nextMobileLayout !== mobileLayout || nextSmallMobileLayout !== smallMobileLayout;
   mobileLayout = nextMobileLayout;
+  smallMobileLayout = nextSmallMobileLayout;
   toggleBodyClass("spiral-mobile-layout", mobileLayout);
+  toggleBodyClass("spiral-small-mobile-layout", smallMobileLayout);
   toggleBodyClass("spiral-coarse-pointer", coarsePointerQuery?.matches === true);
   if (!compactNavigationQuery?.matches) setMobileMenu(false);
   if (changed) {
@@ -682,6 +688,7 @@
 window.addEventListener("resize", scheduleResize, { passive: true });
 reducedMotionQuery?.addEventListener?.("change", updateReducedState);
 mobileLayoutQuery?.addEventListener?.("change", scheduleResize);
+smallMobileLayoutQuery?.addEventListener?.("change", scheduleResize);
 coarsePointerQuery?.addEventListener?.("change", updateResponsiveMode);
 compactNavigationQuery?.addEventListener?.("change", updateResponsiveMode);
 
