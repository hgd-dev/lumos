diff --git a/CHANGELOG.md b/CHANGELOG.md
index c38f949777cf053892cc302da22c08fdb407a59d..92731ff68746c144ed5c6309fdbebc4255b9b14d 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,16 @@
 # Changelog
 
+## [3.2.7] - 2026-07-29
+
+### Refined
+
+- Removed the orbiting ghost-word and transition-label layers from the unlinked `home-spiral.html` experiment, eliminating the faint background echo/watermark visible behind the geometric core.
+- Reduced motion overhead through cached scroll geometry and visible-scene-only DOM updates, so distant scenes no longer receive unnecessary per-frame style writes.
+- Tightened the six scene centers and shortened the desktop and mobile pinned scroll tracks for faster movement between chapters.
+- Expanded the near-scene snap radius and shortened its release delay so scenes settle into place from a wider but still bounded interval.
+- Changed shared desktop header dropdowns to open and close on hover while preserving keyboard focus behavior and touch/click fallback.
+- Preserved the brighter cosmic background, axes, triangular core, stable public Home, every workspace, and all scientific behavior.
+
 ## [3.2.6] - 2026-07-29
 
 ### Refined
diff --git a/CITATION.cff b/CITATION.cff
index ab200102e261ff3697c31c72fe82fd9caddf4367..1be9f2502af5cf24d41f8dcfff884bf3b7aa3e80 100644
--- a/CITATION.cff
+++ b/CITATION.cff
@@ -2,7 +2,7 @@ cff-version: 1.2.0
 message: "If you use LUMOS in research, cite the software and the associated paper or preprint when available."
 title: "LUMOS: Localized Unified Monitoring Optimization System"
 type: software
-version: 3.2.6
+version: 3.2.7
 date-released: 2026-07-29
 license: MIT
 abstract: "A stable public socially constrained sequential Bayesian framework and browser-based tool for Heat, Air, Soil, and Water monitoring design, validation, intervention evaluation, cross-domain funding, robust multi-round policy analysis, verified deployment, field campaigns, live outcome tracking, commissioning, calibration, maintenance, and replacement planning."
diff --git a/css/home-spiral.css b/css/home-spiral.css
index 1b0493955ef03c89fe5c5f7e6a5a007f7dfa5c9b..847db5ce997270624ad779ca1763f83f2a69179c 100644
--- a/css/home-spiral.css
+++ b/css/home-spiral.css
@@ -91,8 +91,8 @@ body.home-spiral-page::before {
 
 .spiral-scroll-track {
   position: relative;
-  height: 760vh;
-  min-height: 5480px;
+  height: 650vh;
+  min-height: 4700px;
   background: #020304;
 }
 .spiral-sticky-stage {
@@ -105,10 +105,10 @@ body.home-spiral-page::before {
   isolation: isolate;
   opacity: var(--motion-stage-opacity);
   background:
-    radial-gradient(circle at 50% 46%, rgba(var(--motion-accent-rgb), .16), transparent 33%),
-    radial-gradient(circle at 16% 18%, rgba(78, 110, 255, .18), transparent 34%),
-    radial-gradient(circle at 86% 72%, rgba(67, 222, 196, .14), transparent 38%),
-    radial-gradient(circle at 48% 88%, rgba(255,255,255,.045), transparent 32%),
+    radial-gradient(circle at 50% 46%, rgba(var(--motion-accent-rgb), .18), transparent 34%),
+    radial-gradient(circle at 16% 18%, rgba(78, 110, 255, .2), transparent 35%),
+    radial-gradient(circle at 86% 72%, rgba(67, 222, 196, .16), transparent 39%),
+    radial-gradient(circle at 48% 88%, rgba(255,255,255,.05), transparent 33%),
     #020304;
   transition: opacity 90ms linear;
 }
@@ -181,8 +181,8 @@ body.home-spiral-page::before {
   width: min(76vw, 1080px);
   aspect-ratio: 1;
   border-radius: 50%;
-  filter: blur(148px);
-  opacity: .3;
+  filter: blur(112px);
+  opacity: .34;
   mix-blend-mode: screen;
   transform: translate3d(0, 0, 0);
 }
@@ -233,7 +233,7 @@ body.home-spiral-page::before {
     rotateZ(var(--motion-core-rotation));
   transform-style: preserve-3d;
   opacity: calc(.62 + var(--motion-gap) * .24);
-  filter: drop-shadow(0 0 34px rgba(var(--motion-accent-rgb), .32));
+  filter: drop-shadow(0 0 28px rgba(var(--motion-accent-rgb), .34));
 }
 .motion-core svg { width: 100%; height: 100%; overflow: visible; }
 .motion-core path,
@@ -256,55 +256,6 @@ body.home-spiral-page::before {
   stroke-width: .9;
 }
 
-.motion-ghost-orbit {
-  position: absolute;
-  inset: 0;
-  z-index: 7;
-  transform-style: preserve-3d;
-  contain: layout paint style;
-}
-.motion-ghost-orbit span {
-  position: absolute;
-  left: 50%;
-  top: 50%;
-  width: max-content;
-  color: transparent;
-  -webkit-text-stroke: 1px rgba(var(--motion-accent-rgb), .26);
-  font-family: "Orbitron", var(--font-display);
-  font-size: clamp(5rem, 14vw, 14rem);
-  font-weight: 700;
-  letter-spacing: .12em;
-  line-height: .8;
-  opacity: 0;
-  filter: blur(2px);
-  transform: translate3d(-50%, -50%, -300px);
-  will-change: transform, opacity;
-  contain: layout paint style;
-}
-
-.motion-ghost-label {
-  position: absolute;
-  left: 50%;
-  top: 49%;
-  z-index: 7;
-  width: max-content;
-  max-width: 92vw;
-  color: transparent;
-  -webkit-text-stroke: 1px rgba(var(--motion-accent-rgb), .44);
-  font-family: "Orbitron", var(--font-display);
-  font-size: clamp(4.5rem, 12vw, 11rem);
-  font-weight: 700;
-  letter-spacing: .08em;
-  line-height: .78;
-  text-align: center;
-  text-transform: uppercase;
-  opacity: 0;
-  filter: blur(1.5px);
-  transform: translate3d(-50%, -50%, -180px) rotateZ(-8deg) scale(.92);
-  will-change: transform, opacity;
-  contain: layout paint style;
-}
-
 
 
 .motion-micro-ui {
@@ -816,7 +767,7 @@ body:not(.spiral-handoff-ready) .spiral-after-content { pointer-events:none; }
 }
 
 @media (max-width: 780px) {
-  .spiral-scroll-track { height:690vh;min-height:4300px; }
+  .spiral-scroll-track { height:590vh;min-height:3700px; }
   .home-spiral-header { min-height:56px;padding-inline:16px; }
   .home-spiral-header .header-actions { margin-left:auto; }
   .home-spiral-header #systemCheckButton,.home-spiral-experiment-chip { display:none; }
@@ -862,8 +813,7 @@ body:not(.spiral-handoff-ready) .spiral-after-content { pointer-events:none; }
   .spiral-scroll-track { height:auto;min-height:0; }
   .spiral-sticky-stage { position:relative;height:auto;min-height:0;overflow:visible;padding:90px 20px 70px; }
   .motion-background { position:fixed;opacity:.35; }
-  .motion-core,.motion-blueprint-grid,.motion-ghost-orbit,.motion-chroma { transform:none!important;animation:none!important; }
-  .motion-ghost-label { display:none; }
+  .motion-core,.motion-blueprint-grid,.motion-chroma { transform:none!important;animation:none!important; }
   .motion-micro-ui,.spiral-scroll-cue,.spiral-progress { display:none; }
   .spiral-viewport { position:relative;inset:auto;overflow:visible;perspective:none; }
   .spiral-orbit { position:relative;display:grid;gap:80px;transform:none!important; }
diff --git a/css/styles.css b/css/styles.css
index d3db7e0becd12e6d5bbe6630f90d45af773d6006..89b85870325c7f182a3f66ec3bb7cc53767db368 100644
--- a/css/styles.css
+++ b/css/styles.css
@@ -3090,3 +3090,17 @@ body.header-collapsed .header-collapse-toggle,
   .about-current-work,
   .about-cta { align-items: flex-start; flex-direction: column; }
 }
+
+
+@media (hover: hover) and (pointer: fine) {
+  .site-menu > summary { cursor: default; }
+  .site-menu .workspace-menu-panel {
+    transform-origin: top center;
+    animation: siteMenuHoverIn 120ms ease-out both;
+  }
+}
+
+@keyframes siteMenuHoverIn {
+  from { opacity: 0; transform: translate(-50%, -4px) scale(.985); }
+  to { opacity: 1; transform: translate(-50%, 0) scale(1); }
+}
diff --git a/data/examples/public-release-readiness.csv b/data/examples/public-release-readiness.csv
index 60dec29392debcebf3c5bafcdbdb10dd7607f3f3..ed347b1937ced0d45c6ffca4cba84877741d6177 100644
--- a/data/examples/public-release-readiness.csv
+++ b/data/examples/public-release-readiness.csv
@@ -1,21 +1,21 @@
 version,checksum,category,check_id,label,status,detail
-3.1.8,f08ebafc,release,release-version,Release metadata identity,pass,"Release metadata, citation, and packaged assets identify one consistent release."
-3.1.8,f08ebafc,release,release-status,Stable public release channel,pass,Release status: stable-public-v3.
-3.1.8,f08ebafc,architecture,domain-coverage,Four public scientific adapters,pass,"Declared domains: heat, air, soil, water."
-3.1.8,f08ebafc,architecture,workspace-coverage,Complete unified decision lifecycle,pass,10/10 required unified workspaces declared.
-3.1.8,f08ebafc,science,domain-audit,Cross-domain scientific audit,pass,"86 passes, 0 warnings, 0 failures; checksum 6be571eb."
-3.1.8,f08ebafc,operations,commissioning-example,Commissioning and maintenance evidence,pass,35/42 commissioned; 3 protected failures; 0 unresolved; checksum 6318a385.
-3.1.8,f08ebafc,science,capability-contract,Required public capabilities,pass,11 mandatory public-adapter capabilities are registered.
-3.1.8,f08ebafc,reproducibility,evidence-manifest,Frozen evidence artifact manifest,pass,11/11 required evidence artifacts declared.
-3.1.8,f08ebafc,governance,governance-docs,"Limitations, privacy, security, citation, and license",pass,5/5 governance documents declared.
-3.1.8,f08ebafc,governance,limitations-scope,Limitations and intended-use guidance,pass,Release metadata requires a centralized limitations and intended-use record.
-3.1.8,f08ebafc,accessibility,accessibility-contract,Public accessibility contract,pass,10/10 required accessibility features declared.
-3.1.8,f08ebafc,accessibility,runtime-layout,Runtime map-expansion controls,pass,Header collapse available; panel collapse available; map focus available.
-3.1.8,f08ebafc,accessibility,runtime-a11y,Runtime keyboard and motion controls,pass,"Skip navigation, visible focus, reduced-motion, and color-vision-safe controls are required in the application shell."
-3.1.8,f08ebafc,accessibility,runtime-home,Dedicated public Home navigation,pass,A dedicated Home entry point introduces the system and routes users into each scientific workspace.
-3.1.8,f08ebafc,governance,runtime-documentation,In-application documentation center,pass,Core public documentation is available inside an accessible application dialog.
-3.1.8,f08ebafc,governance,runtime-about,About Us and team attribution,pass,The in-application About Us page and team attribution are declared.
-3.1.8,f08ebafc,deployment,static-hosting,Static public deployment,pass,Hosting contract: static-github-pages.
-3.1.8,f08ebafc,deployment,offline-shell,Offline application shell,pass,Versioned service-worker application shell declared.
-3.1.8,f08ebafc,security,credential-boundary,Frontend credential boundary,pass,No permanent API credentials are permitted in the public application bundle.
-3.1.8,f08ebafc,governance,license,Open-source license,pass,Declared license: MIT.
+3.2.7,ddc44d64,release,release-version,Release metadata identity,pass,"Release metadata, citation, and packaged assets identify one consistent release."
+3.2.7,ddc44d64,release,release-status,Stable public release channel,pass,Release status: stable-public-v3.
+3.2.7,ddc44d64,architecture,domain-coverage,Four public scientific adapters,pass,"Declared domains: heat, air, soil, water."
+3.2.7,ddc44d64,architecture,workspace-coverage,Complete unified decision lifecycle,pass,10/10 required unified workspaces declared.
+3.2.7,ddc44d64,science,domain-audit,Cross-domain scientific audit,pass,"86 passes, 0 warnings, 0 failures; checksum 6be571eb."
+3.2.7,ddc44d64,operations,commissioning-example,Commissioning and maintenance evidence,pass,35/42 commissioned; 3 protected failures; 0 unresolved; checksum 6318a385.
+3.2.7,ddc44d64,science,capability-contract,Required public capabilities,pass,11 mandatory public-adapter capabilities are registered.
+3.2.7,ddc44d64,reproducibility,evidence-manifest,Frozen evidence artifact manifest,pass,11/11 required evidence artifacts declared.
+3.2.7,ddc44d64,governance,governance-docs,"Limitations, privacy, security, citation, and license",pass,5/5 governance documents declared.
+3.2.7,ddc44d64,governance,limitations-scope,Limitations and intended-use guidance,pass,Release metadata requires a centralized limitations and intended-use record.
+3.2.7,ddc44d64,accessibility,accessibility-contract,Public accessibility contract,pass,10/10 required accessibility features declared.
+3.2.7,ddc44d64,accessibility,runtime-layout,Runtime map-expansion controls,pass,Header collapse available; panel collapse available; map focus available.
+3.2.7,ddc44d64,accessibility,runtime-a11y,Runtime keyboard and motion controls,pass,"Skip navigation, visible focus, reduced-motion, and color-vision-safe controls are required in the application shell."
+3.2.7,ddc44d64,accessibility,runtime-home,Dedicated public Home navigation,pass,A dedicated Home entry point introduces the system and routes users into each scientific workspace.
+3.2.7,ddc44d64,governance,runtime-documentation,In-application documentation center,pass,Core public documentation is available inside an accessible application dialog.
+3.2.7,ddc44d64,governance,runtime-about,About Us and team attribution,pass,The in-application About Us page and team attribution are declared.
+3.2.7,ddc44d64,deployment,static-hosting,Static public deployment,pass,Hosting contract: static-github-pages.
+3.2.7,ddc44d64,deployment,offline-shell,Offline application shell,pass,Versioned service-worker application shell declared.
+3.2.7,ddc44d64,security,credential-boundary,Frontend credential boundary,pass,No permanent API credentials are permitted in the public application bundle.
+3.2.7,ddc44d64,governance,license,Open-source license,pass,Declared license: MIT.
diff --git a/data/examples/public-release-readiness.json b/data/examples/public-release-readiness.json
index f2928aa0612ca7bd3147c5358c60b22012050b28..2973985d80110846acba72ad21bceafa9c6ba517 100644
--- a/data/examples/public-release-readiness.json
+++ b/data/examples/public-release-readiness.json
@@ -2,7 +2,7 @@
   "schemaVersion": "1.0",
   "architecture": "LUMOS-v3-internal-release-quality-audit",
   "generatedAt": "1970-01-01T00:00:00.000Z",
-  "version": "3.1.8",
+  "version": "3.2.7",
   "checks": [
     {
       "id": "release-version",
@@ -152,5 +152,5 @@
   },
   "ready": true,
   "claimBoundary": "This readiness audit verifies declared software, evidence, accessibility, governance, and release contracts. It does not certify environmental accuracy, cybersecurity, regulatory compliance, accessibility conformance, field deployment, or institutional approval.",
-  "checksum": "f08ebafc"
+  "checksum": "ddc44d64"
 }
diff --git a/docs/PUBLIC_RELEASE.md b/docs/PUBLIC_RELEASE.md
index 3457b3a4a154226da92a3ec0d6879ee156645299..08ed1fadb861151b6f66d90e7a34f8618c73bfc6 100644
--- a/docs/PUBLIC_RELEASE.md
+++ b/docs/PUBLIC_RELEASE.md
@@ -1,12 +1,10 @@
-# LUMOS 3.2.6 Multi-Page Public Release
+# LUMOS 3.2.7 Multi-Page Public Release
 
-## v3.2.6 simplified cosmic stage and tighter editorial motion
+## v3.2.7 watermark-free stage, faster pacing, and hover navigation
 
-The unlinked `home-spiral.html` route no longer includes the three-dimensional film-strip history. Removing that background system simplifies the composition and eliminates its per-frame transform, opacity, filter, and depth calculations.
+The unlinked `home-spiral.html` route now removes the former ghost-word orbit and transition label that could read as an unintended echo or watermark behind the main triangle. The luminous axes, wireframe core, particles, blueprint grid, and scene-colored glow remain.
 
-The persistent environment is stronger instead: brighter construction axes, a more visible triangular core, clearer guide and signal lines, stronger node glow, a brighter blueprint grid, and larger scene-reactive chromatic fields. Particle count is reduced while the static nebula treatment is stronger, producing a fuller space-and-technology atmosphere at lower runtime cost.
-
-Scene centers and the total pinned scroll distance are slightly closer together, so transitions feel more connected while retaining a foreground-free ambient gap.
+The motion loop now caches scroll geometry and updates only scenes that are actually near the viewport. Scene centers and total pinned travel are tighter, while the snap radius is wider and the snap delay shorter. Shared desktop navigation dropdowns open and close on hover, with keyboard focus and touch/click fallback preserved.
 
 No scientific model, objective, constraint, evidence generator, stable public page, or workspace behavior changed.
 
@@ -89,7 +87,7 @@ See `docs/COMMISSIONING_AND_MAINTENANCE.md`, `docs/LIMITATIONS.md`, and `docs/PR
 
 ## Brand and Home refinement
 
-The 3.2.6 public architecture uses separate static entry pages for Home, About Us, Documentation, Research & Process, Contact & Feedback, Unified, Heat, Air, Soil, and Water. A shared workspace shell and shared JavaScript model preserve one scientific implementation across every workspace URL, while lightweight informational pages keep public navigation clear. New sessions open at the full United States extent, while saved workspaces are restored only by explicit user action.
+The 3.2.7 public architecture uses separate static entry pages for Home, About Us, Documentation, Research & Process, Contact & Feedback, Unified, Heat, Air, Soil, and Water. A shared workspace shell and shared JavaScript model preserve one scientific implementation across every workspace URL, while lightweight informational pages keep public navigation clear. New sessions open at the full United States extent, while saved workspaces are restored only by explicit user action.
 
 
 ## v3.1.4 information architecture
diff --git a/docs/RELEASE_CHECKLIST.md b/docs/RELEASE_CHECKLIST.md
index 393997cb382df9d4dc15713e2f41a7ab28756180..23c559bb4f995e2e0fa634a1024c1b9e4c41e75e 100644
--- a/docs/RELEASE_CHECKLIST.md
+++ b/docs/RELEASE_CHECKLIST.md
@@ -2,9 +2,9 @@
 
 ## Version and package
 
-- [ ] `package.json`, `release.json`, `js/release/version.js`, service worker, citation metadata, release notes, and changelog identify `3.2.6`. The public masthead, browser title, footer, manifest name, README title, and model-specification title remain version-neutral.
+- [ ] `package.json`, `release.json`, `js/release/version.js`, service worker, citation metadata, release notes, and changelog identify `3.2.7`. The public masthead, browser title, footer, manifest name, README title, and model-specification title remain version-neutral.
 - [ ] Release status is `stable-public-v3`.
-- [ ] Service-worker cache is `lumos-v3.2.6` and contains every standard public entry page, the unlinked `home-3d.html` and `home-spiral.html` experiments, the shared workspace shell, all public domain modules, unified operations, and documentation scripts.
+- [ ] Service-worker cache is `lumos-v3.2.7` and contains every standard public entry page, the unlinked `home-3d.html` and `home-spiral.html` experiments, the shared workspace shell, all public domain modules, unified operations, and documentation scripts.
 - [ ] No permanent credential is embedded in public assets.
 
 ## Automated verification
diff --git a/home-spiral.html b/home-spiral.html
index bc1a2c7912ec22db131deb305a4dd22bf6020c38..938d87d2d4a23266e8f67a2da07c44ec5c0c709f 100644
--- a/home-spiral.html
+++ b/home-spiral.html
@@ -61,15 +61,6 @@
           <div class="motion-axis motion-axis-horizontal"></div>
           <div class="motion-axis motion-axis-diagonal"></div>
           <div class="motion-axis motion-axis-vertical"></div>
-          <div class="motion-ghost-orbit" id="motionGhostOrbit">
-            <span data-ghost="LUMOS">LUMOS</span>
-            <span data-ghost="UNIFIED">UNIFIED</span>
-            <span data-ghost="HEAT">HEAT</span>
-            <span data-ghost="AIR">AIR</span>
-            <span data-ghost="WATER">WATER</span>
-            <span data-ghost="SOIL">SOIL</span>
-          </div>
-          <div id="motionGhostLabel" class="motion-ghost-label">LUMOS</div>
           <div class="motion-core" id="motionCore">
             <svg viewBox="0 0 600 600" role="presentation">
               <g class="core-guides">
diff --git a/js/home-spiral.js b/js/home-spiral.js
index d238a054e20f434876fde3287a01ceeefe1d42f5..34582d0a14f1c93c01b4ee4ea8dd23628104cbb7 100644
--- a/js/home-spiral.js
+++ b/js/home-spiral.js
@@ -2,8 +2,6 @@ const track = document.querySelector("#spiralSequence");
 const viewport = document.querySelector("#spiralViewport");
 const orbit = document.querySelector("#spiralOrbit");
 const cards = Array.from(document.querySelectorAll(".spiral-item"));
-const ghostItems = Array.from(document.querySelectorAll("#motionGhostOrbit > span"));
-const ghostLabel = document.querySelector("#motionGhostLabel");
 const typingWord = document.querySelector("#motionTypingWord");
 const particleCanvas = document.querySelector("#spiralParticleCanvas");
 const counter = document.querySelector("#spiralCounter");
@@ -16,18 +14,18 @@ const rootStyle = document.documentElement.style;
 const reducedMotionQuery = window.matchMedia?.("(prefers-reduced-motion: reduce)");
 const coarsePointerQuery = window.matchMedia?.("(pointer: coarse)");
 
-const SCENE_CENTERS = [0, 0.16, 0.32, 0.48, 0.64, 0.8];
+const SCENE_CENTERS = [0, 0.145, 0.29, 0.435, 0.58, 0.725];
 const SCENE_INNER_RADIUS = 0.019;
-const SCENE_OUTER_RADIUS = 0.051;
-const SCENE_INTERVAL = 0.16;
-const SNAP_RADIUS = 0.03;
-const SNAP_DELAY = 145;
-const STAGE_BLACK_START = 0.92;
-const STAGE_BLACK_END = 0.945;
-const STAGE_FADE_START = 0.95;
-const STAGE_FADE_END = 0.972;
-const CONTENT_REVEAL_START = 0.975;
-const CONTENT_REVEAL_END = 0.998;
+const SCENE_OUTER_RADIUS = 0.048;
+const SCENE_INTERVAL = 0.145;
+const SNAP_RADIUS = 0.045;
+const SNAP_DELAY = 125;
+const STAGE_BLACK_START = 0.84;
+const STAGE_BLACK_END = 0.875;
+const STAGE_FADE_START = 0.88;
+const STAGE_FADE_END = 0.915;
+const CONTENT_REVEAL_START = 0.92;
+const CONTENT_REVEAL_END = 0.975;
 const TYPING_WORDS = ["monitoring", "intervention", "planning", "optimization", "deployment", "evaluation"];
 
 let activeCard = -1;
@@ -41,7 +39,9 @@ let targetPointerX = 0;
 let targetPointerY = 0;
 let pointerX = 0;
 let pointerY = 0;
-let lastProgress = -1;
+let scrollStart = 0;
+let scrollRange = 1;
+const cardVisibility = cards.map(() => false);
 let typingTimer = 0;
 let typingWordIndex = 0;
 let typingCharacterIndex = TYPING_WORDS[0].length;
@@ -54,15 +54,18 @@ const smoothstep = (value) => {
 };
 const mix = (a, b, t) => a + (b - a) * t;
 
-function scrollMetrics() {
-  if (!track) return { start: 0, range: 1 };
-  const start = track.offsetTop;
-  return { start, range: Math.max(1, track.offsetHeight - window.innerHeight) };
+function refreshScrollMetrics() {
+  if (!track) {
+    scrollStart = 0;
+    scrollRange = 1;
+    return;
+  }
+  scrollStart = track.offsetTop;
+  scrollRange = Math.max(1, track.offsetHeight - window.innerHeight);
 }
 
 function scrollProgress() {
-  const { start, range } = scrollMetrics();
-  return clamp((window.scrollY - start) / range);
+  return clamp((window.scrollY - scrollStart) / scrollRange);
 }
 
 function sceneCenter(index) {
@@ -84,8 +87,7 @@ function nearestScene(progress) {
 }
 
 function scrollToScene(index, behavior = "smooth") {
-  const { start, range } = scrollMetrics();
-  window.scrollTo({ top: start + sceneCenter(index) * range, behavior });
+  window.scrollTo({ top: scrollStart + sceneCenter(index) * scrollRange, behavior });
 }
 
 function createProgressControls() {
@@ -135,25 +137,6 @@ function cardOpacity(distance) {
   return 1 - smoothstep((absolute - SCENE_INNER_RADIUS) / (SCENE_OUTER_RADIUS - SCENE_INNER_RADIUS));
 }
 
-function renderGhostOrbit(progress, gapIntensity) {
-  const width = Math.max(1, window.innerWidth);
-  const height = Math.max(1, window.innerHeight);
-  ghostItems.forEach((ghost, index) => {
-    const center = sceneCenter(index);
-    const offset = (progress - center) / SCENE_INTERVAL;
-    const absolute = Math.abs(offset);
-    const angle = offset * 1.06 + index * 0.38 + progress * 1.35;
-    const x = Math.sin(angle) * width * 0.38 + offset * width * -0.08;
-    const y = Math.cos(angle * 0.84) * height * 0.23 + offset * height * -0.17;
-    const z = -260 - absolute * 300;
-    const proximity = clamp(1 - absolute * 0.5, 0, 1);
-    const opacity = (0.012 + gapIntensity * 0.17) * proximity;
-    ghost.style.opacity = opacity.toFixed(3);
-    ghost.style.visibility = opacity > 0.004 ? "visible" : "hidden";
-    ghost.style.transform = `translate3d(calc(-50% + ${x.toFixed(1)}px), calc(-50% + ${y.toFixed(1)}px), ${z.toFixed(1)}px) rotateY(${(offset * -28).toFixed(2)}deg) rotateZ(${(angle * 12).toFixed(2)}deg)`;
-  });
-}
-
 function renderMotion(force = false) {
   frameRequested = false;
   if (reduced || !track || !viewport || !orbit) return;
@@ -185,39 +168,31 @@ function renderMotion(force = false) {
     const rotateY = travel * 9.5;
     const rotateZ = travel * -7.5;
     const scale = clamp(1 - absoluteNormalized * 0.12, 0.76, 1);
-    const visible = opacity > 0.002 && Math.abs(distance) < SCENE_OUTER_RADIUS + 0.016;
+    const visible = opacity > 0.002 && Math.abs(distance) < SCENE_OUTER_RADIUS + 0.014;
+
+    if (!visible) {
+      if (cardVisibility[index]) {
+        card.style.opacity = "0";
+        card.style.visibility = "hidden";
+        cardVisibility[index] = false;
+      }
+      return;
+    }
 
+    cardVisibility[index] = true;
     card.style.transform = `translate3d(${x.toFixed(1)}px, ${y.toFixed(1)}px, ${z.toFixed(1)}px) rotateX(${rotateX.toFixed(2)}deg) rotateY(${rotateY.toFixed(2)}deg) rotateZ(${rotateZ.toFixed(2)}deg) scale(${scale.toFixed(4)})`;
     card.style.opacity = opacity.toFixed(3);
-    card.style.visibility = visible ? "visible" : "hidden";
+    card.style.visibility = "visible";
     card.style.zIndex = String(50 - Math.round(absoluteNormalized * 10));
-    card.style.setProperty("--scene-copy-x", `${(travel * width * 0.025).toFixed(1)}px`);
-    card.style.setProperty("--scene-copy-y", `${(travel * height * 0.018).toFixed(1)}px`);
-    card.style.setProperty("--scene-plane-x", `${(travel * width * -0.055).toFixed(1)}px`);
-    card.style.setProperty("--scene-plane-y", `${(travel * height * -0.035).toFixed(1)}px`);
+    card.style.setProperty("--scene-copy-x", `${(travel * width * 0.022).toFixed(1)}px`);
+    card.style.setProperty("--scene-copy-y", `${(travel * height * 0.015).toFixed(1)}px`);
+    card.style.setProperty("--scene-plane-x", `${(travel * width * -0.048).toFixed(1)}px`);
+    card.style.setProperty("--scene-plane-y", `${(travel * height * -0.03).toFixed(1)}px`);
   });
 
   const gapIntensity = clamp(1 - maximumOpacity);
   const { index: nearest } = nearestScene(progress);
   setActiveCard(nearest);
-  renderGhostOrbit(progress, gapIntensity);
-  if (ghostLabel) {
-    let leftIndex = nearest;
-    let rightIndex = nearest;
-    for (let index = 0; index < cards.length - 1; index += 1) {
-      if (progress >= sceneCenter(index) && progress <= sceneCenter(index + 1)) {
-        leftIndex = index;
-        rightIndex = index + 1;
-        break;
-      }
-    }
-    ghostLabel.textContent = leftIndex === rightIndex
-      ? (cards[nearest]?.dataset.spiralTitle || "LUMOS")
-      : `${cards[leftIndex]?.dataset.spiralTitle || "LUMOS"} / ${cards[rightIndex]?.dataset.spiralTitle || "LUMOS"}`;
-    ghostLabel.style.opacity = (gapIntensity * 0.28).toFixed(3);
-    ghostLabel.style.transform = `translate3d(-50%, -50%, -180px) rotateZ(${(-8 + progress * 13).toFixed(2)}deg) scale(${(0.9 + gapIntensity * 0.08).toFixed(3)})`;
-  }
-
   const blackOverlay = smoothstep((progress - STAGE_BLACK_START) / (STAGE_BLACK_END - STAGE_BLACK_START));
   const stageFade = 1 - smoothstep((progress - STAGE_FADE_START) / (STAGE_FADE_END - STAGE_FADE_START));
   const contentReveal = smoothstep((progress - CONTENT_REVEAL_START) / (CONTENT_REVEAL_END - CONTENT_REVEAL_START));
@@ -238,7 +213,6 @@ function renderMotion(force = false) {
   document.body.classList.toggle("spiral-handoff-active", progress >= STAGE_BLACK_START);
   document.body.classList.toggle("spiral-handoff-ready", contentReveal > 0.82);
 
-  lastProgress = progress;
   if (
     Math.abs(pointerX - targetPointerX) > 0.012 ||
     Math.abs(pointerY - targetPointerY) > 0.012
@@ -256,8 +230,8 @@ function maybeSnapToScene() {
   const progress = scrollProgress();
   if (progress < SCENE_CENTERS[0] - SNAP_RADIUS || progress > SCENE_CENTERS.at(-1) + SNAP_RADIUS) return;
   const { index, distance } = nearestScene(progress);
-  if (distance > 0.0015 && distance <= SNAP_RADIUS) {
-    snappingUntil = Date.now() + 620;
+  if (distance > 0.001 && distance <= SNAP_RADIUS) {
+    snappingUntil = Date.now() + 560;
     scrollToScene(index, "smooth");
   }
 }
@@ -408,15 +382,6 @@ function clearMotionStyles() {
     card.setAttribute("aria-hidden", "false");
     setInteractiveState(card, true);
   });
-  ghostItems.forEach((ghost) => {
-    ghost.style.removeProperty("transform");
-    ghost.style.removeProperty("opacity");
-    ghost.style.removeProperty("visibility");
-  });
-  if (ghostLabel) {
-    ghostLabel.style.removeProperty("transform");
-    ghostLabel.style.removeProperty("opacity");
-  }
 }
 
 function updateReducedState() {
@@ -456,6 +421,7 @@ function scheduleResize() {
   if (resizeFrame) return;
   resizeFrame = window.requestAnimationFrame(() => {
     resizeFrame = 0;
+    refreshScrollMetrics();
     drawParticleField();
     renderMotion(true);
   });
@@ -465,6 +431,7 @@ window.addEventListener("scroll", updateFromScroll, { passive: true });
 window.addEventListener("resize", scheduleResize, { passive: true });
 reducedMotionQuery?.addEventListener?.("change", updateReducedState);
 
+refreshScrollMetrics();
 createProgressControls();
 initializeTypingIdentity();
 drawParticleField();
diff --git a/js/release/documentation.js b/js/release/documentation.js
index 6f44af7f0b453e2b54b6cf29f4c321184a80bb91..448d4a674db1c6fce43d8c36ea36cf66851d07f8 100644
--- a/js/release/documentation.js
+++ b/js/release/documentation.js
@@ -116,17 +116,17 @@ export const DOCUMENTATION_PAGES = Object.freeze({
   }),
   "release-notes": Object.freeze({
     title: "Release notes",
-    kicker: "Current public release: 3.2.6",
-    summary: "No film strip, stronger ambient geometry, lower runtime overhead, and tighter scene spacing.",
+    kicker: "Current public release: 3.2.7",
+    summary: "A watermark-free cosmic stage, faster scene pacing, wider snap behavior, and hover-driven desktop navigation.",
     html: `
-      <p><strong>LUMOS 3.2.6</strong> further refines the unlinked <code>home-spiral.html</code> experiment by removing the film-strip history, strengthening the luminous geometric background, reducing motion overhead, and tightening scene spacing.</p>
+      <p><strong>LUMOS 3.2.7</strong> further refines the unlinked <code>home-spiral.html</code> experiment and the shared public navigation without changing the stable Home or any scientific workspace.</p>
       <h3>Experimental presentation</h3>
       <ul>
-        <li>Removed the three-dimensional film-strip history and its per-frame renderer.</li>
-        <li>Brightened the starfield, chromatic glow, blueprint grid, axes, triangular core, guide lines, signal strokes, and nodes.</li>
-        <li>Reduced runtime filter work, pointer follow-up frames, and particle cost while preserving a stronger cosmic atmosphere.</li>
-        <li>Shortened scene-center spacing and the pinned scroll distance for a more connected sequence.</li>
-        <li>Kept the stable Home and every scientific workspace unchanged.</li>
+        <li>Removed the faint orbiting ghost-word and transition-label layers that appeared as an unintended background watermark.</li>
+        <li>Reduced per-frame work through cached scroll geometry and visible-scene-only DOM updates.</li>
+        <li>Tightened the six scene centers, shortened the pinned scroll track, and widened the near-scene snap range.</li>
+        <li>Kept the brighter axes, triangle, particles, blueprint field, and scene-colored glow.</li>
+        <li>Changed desktop top-bar dropdowns to open and close on hover while retaining focus, keyboard, and touch behavior.</li>
       </ul>
       <h3>Scientific scope</h3>
       <p>No model, objective, constraint, adapter, evidence generator, validation protocol, or public claim boundary changed.</p>
diff --git a/js/release/public-readiness.js b/js/release/public-readiness.js
index fc08aa6d151c3948b2eeb2338b3cd4d3bd217fe3..1674b77572aa109dc48a601a68b2d871cc7e059e 100644
--- a/js/release/public-readiness.js
+++ b/js/release/public-readiness.js
@@ -93,7 +93,7 @@ export function runPublicLaunchReadiness(options = {}) {
 
   checks.push(readinessCheck({
     id: "release-version", category: "release", label: "Release metadata identity",
-    satisfied: release.version === "3.2.6",
+    satisfied: release.version === "3.2.7",
     detail: release.version ? "Release metadata, citation, and packaged assets identify one consistent release." : "Release metadata does not identify a packaged release."
   }));
   checks.push(readinessCheck({
diff --git a/js/release/version.js b/js/release/version.js
index ff2416aac6bea08ea9e501df05a17ed82251a89b..15e186c115a6d3c5ab4a0d741cf71224844d79a2 100644
--- a/js/release/version.js
+++ b/js/release/version.js
@@ -1,4 +1,4 @@
 export const APP_NAME = "LUMOS";
-export const APP_VERSION = "3.2.6";
+export const APP_VERSION = "3.2.7";
 export const RELEASE_CHANNEL = "stable-public";
 export const RELEASE_DATE = "2026-07-29";
diff --git a/js/site.js b/js/site.js
index 711a3db81a6306d2440694ac0ffc94a31e436f2f..398468a5533cfb084714b8fde6098355774458f0 100644
--- a/js/site.js
+++ b/js/site.js
@@ -163,12 +163,38 @@ async function registerServiceWorker() {
 
 function initializeWorkspaceMenus() {
   const menus = [...document.querySelectorAll(".site-menu")];
+  const hoverQuery = window.matchMedia?.("(hover: hover) and (pointer: fine)");
+  const closeOthers = (activeMenu) => {
+    for (const other of menus) if (other !== activeMenu) other.removeAttribute("open");
+  };
+
   for (const menu of menus) {
+    const summary = menu.querySelector(":scope > summary");
     menu.addEventListener("toggle", () => {
-      if (!menu.open) return;
-      for (const other of menus) if (other !== menu) other.removeAttribute("open");
+      if (menu.open) closeOthers(menu);
+    });
+    menu.addEventListener("mouseenter", () => {
+      if (!hoverQuery?.matches) return;
+      closeOthers(menu);
+      menu.setAttribute("open", "");
+    });
+    menu.addEventListener("mouseleave", () => {
+      if (hoverQuery?.matches && !menu.contains(document.activeElement)) menu.removeAttribute("open");
+    });
+    menu.addEventListener("focusin", () => {
+      closeOthers(menu);
+      menu.setAttribute("open", "");
+    });
+    menu.addEventListener("focusout", () => {
+      window.setTimeout(() => {
+        if (!menu.contains(document.activeElement) && hoverQuery?.matches) menu.removeAttribute("open");
+      }, 0);
+    });
+    summary?.addEventListener("click", (event) => {
+      if (hoverQuery?.matches && event.detail > 0) event.preventDefault();
     });
   }
+
   document.addEventListener("click", (event) => {
     for (const menu of menus) if (menu.open && !menu.contains(event.target)) menu.removeAttribute("open");
   });
diff --git a/package.json b/package.json
index 5433a156eb69b5a206b1e0e7b285a818c3093b29..b9ecf73238a123d69ab5ca4a9c58f527a29f040f 100644
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "lumos-core",
-  "version": "3.2.6",
+  "version": "3.2.7",
   "private": true,
   "type": "module",
   "scripts": {
diff --git a/release.json b/release.json
index 4518871c335b7af2ddff76adc13924edff2a7ad7..c98e6fc8c5b96c44acf2db50579ba12903c263fe 100644
--- a/release.json
+++ b/release.json
@@ -1,6 +1,6 @@
 {
   "name": "LUMOS",
-  "version": "3.2.6",
+  "version": "3.2.7",
   "releaseDate": "2026-07-29",
   "status": "stable-public-v3",
   "primaryDomain": "multi-domain",
@@ -130,7 +130,7 @@
       "entry": "home-spiral.html",
       "linkedFromPrimaryNavigation": false,
       "indexing": "noindex-nofollow",
-      "renderer": "optimized-native-css-editorial-motion-canvas-with-split-identity-and-unified-scenes-custom-domain-glyphs-and-brightened-ambient-geometric-background",
+      "renderer": "optimized-native-css-editorial-motion-canvas-with-clean-geometric-background-cached-scroll-metrics-and-hover-navigation",
       "interaction": [
         "sticky-pinned-continuous-motion-stage",
         "first-frame-lumos-hero-without-empty-lead-in",
@@ -144,7 +144,11 @@
         "expanded-near-scene-scroll-release-snap",
         "full-background-fade-before-standard-content",
         "pointer-reactive-stage-parallax",
-        "reduced-motion-linear-fallback"
+        "reduced-motion-linear-fallback",
+        "watermark-free-ambient-geometric-background",
+        "cached-scroll-metrics-and-visible-scene-only-rendering",
+        "tighter-six-scene-scroll-sequence",
+        "desktop-hover-navigation-with-keyboard-and-touch-fallback"
       ]
     }
   },
diff --git a/scripts/release-check.mjs b/scripts/release-check.mjs
index 59ea81fbe0f5cf4a8b326dfdef848eb92df645f3..95e37c6751daadd3746320f866965fb926f15191 100644
--- a/scripts/release-check.mjs
+++ b/scripts/release-check.mjs
@@ -5,7 +5,7 @@ import { fileURLToPath } from "node:url";
 import { spawnSync } from "node:child_process";
 
 const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
-const expectedVersion = "3.2.6";
+const expectedVersion = "3.2.7";
 const required = [
   "index.html", "home-3d.html", "home-spiral.html", "about.html", "documentation.html", "research.html", "contact.html", "unified.html", "heat.html", "air.html", "soil.html", "water.html", "workspace-shell.html",
   "package.json", "release.json", "manifest.webmanifest", "service-worker.js", "404.html",
diff --git a/service-worker.js b/service-worker.js
index 6ac36f89167c0324eaea2c49267adb38f1747c54..fbb71dbe556c5e0dc478978fd9b0b8984161813b 100644
--- a/service-worker.js
+++ b/service-worker.js
@@ -1,4 +1,4 @@
-const CACHE_NAME = "lumos-v3.2.6";
+const CACHE_NAME = "lumos-v3.2.7";
 const LIBRARY_ASSETS = [
   "https://unpkg.com/maplibre-gl@5.6.0/dist/maplibre-gl.css",
   "https://unpkg.com/maplibre-gl@5.6.0/dist/maplibre-gl.js",
diff --git a/tests/model.test.js b/tests/model.test.js
index 182ee9e35e7ee8c057337c9bde9301974d3ca971..76434499a49c4b15ce7030cb76f26110653840d6 100644
--- a/tests/model.test.js
+++ b/tests/model.test.js
@@ -2170,7 +2170,7 @@ test("multi-page Unified interface exposes commissioning and full-map controls",
 
 test("cross-domain consistency audit passes and exports reproducible tidy rows", () => {
   const releaseMetadata = {
-    version: "3.2.6",
+    version: "3.2.7",
     status: "stable-public-v3",
     supportedDomains: ["heat", "air", "soil", "water"],
     supportedWorkspaces: [
@@ -2366,6 +2366,8 @@ test("secret motion Home uses a continuous editorial stage and remains unlinked"
   assert.match(spiral, /class="motion-blueprint-grid"/);
   assert.match(spiral, /id="motionTypingWord"/);
   assert.doesNotMatch(spiral, /class="motion-film-orbit"/);
+  assert.doesNotMatch(spiral, /id="motionGhostOrbit"/);
+  assert.doesNotMatch(spiral, /id="motionGhostLabel"/);
   assert.match(spiral, /data-spiral-title="UNIFIED"/);
   assert.match(spiral, /id="spiralCounter">01 \/ 06/);
   assert.match(spiral, /class="domain-glyph"/);
@@ -2383,15 +2385,17 @@ test("secret motion Home uses a continuous editorial stage and remains unlinked"
   assert.match(styles, /transform-style:\s*preserve-3d/);
   assert.match(styles, /motion-display-word/);
   assert.match(styles, /motion-plane-stack/);
-  assert.match(styles, /motion-ghost-orbit/);
+  assert.doesNotMatch(styles, /motion-ghost-orbit/);
   assert.match(styles, /margin-top:\s*-100vh/);
   assert.match(script, /scrollProgress\(\)/);
-  assert.match(script, /const SCENE_CENTERS = \[0, 0\.16, 0\.32, 0\.48, 0\.64, 0\.8\]/);
+  assert.match(script, /refreshScrollMetrics/);
+  assert.match(script, /cardVisibility/);
+  assert.match(script, /const SCENE_CENTERS = \[0, 0\.145, 0\.29, 0\.435, 0\.58, 0\.725\]/);
   assert.match(script, /CONTENT_REVEAL_START/);
   assert.match(script, /TYPING_WORDS/);
-  assert.match(script, /renderGhostOrbit/);
+  assert.doesNotMatch(script, /renderGhostOrbit/);
   assert.doesNotMatch(script, /renderFilmOrbit/);
-  assert.match(script, /const SNAP_RADIUS = 0\.03/);
+  assert.match(script, /const SNAP_RADIUS = 0\.045/);
   assert.doesNotMatch(script, /card\.style\.filter/);
   assert.match(script, /drawParticleField/);
   assert.match(script, /spiral-reduced/);
@@ -2403,7 +2407,7 @@ test("v3 release metadata is internally consistent", async () => {
   const release = JSON.parse(await readFile(new URL("../release.json", import.meta.url), "utf8"));
   const manifest = JSON.parse(await readFile(new URL("../manifest.webmanifest", import.meta.url), "utf8"));
   assert.equal(APP_NAME, "LUMOS");
-  assert.equal(APP_VERSION, "3.2.6");
+  assert.equal(APP_VERSION, "3.2.7");
   assert.equal(RELEASE_CHANNEL, "stable-public");
   assert.equal(packageJson.version, APP_VERSION);
   assert.equal(packageJson.scripts.verify, "node scripts/run-verification.mjs");
@@ -2446,7 +2450,7 @@ test("v3 release metadata is internally consistent", async () => {
 test("v3 service worker preserves the complete same-origin application shell", async () => {
   const { readFile } = await import("node:fs/promises");
   const worker = await readFile(new URL("../service-worker.js", import.meta.url), "utf8");
-  assert.match(worker, /lumos-v3\.2\.6/);
+  assert.match(worker, /lumos-v3\.2\.7/);
   assert.match(worker, /\.\/index\.html/);
   assert.match(worker, /\.\/home-3d\.html/);
   assert.match(worker, /\.\/css\/home-3d\.css/);
@@ -2515,7 +2519,7 @@ test("public Home and permanent documentation page keep release metadata out of
   assert.equal(DEFAULT_DOCUMENTATION_PAGE, "quickstart");
   assert.equal(DOCUMENTATION_ORDER.length, 8);
   assert.ok(DOCUMENTATION_ORDER.every((key) => DOCUMENTATION_PAGES[key]?.title && DOCUMENTATION_PAGES[key]?.html));
-  assert.match(DOCUMENTATION_PAGES["release-notes"].html, /3\.2\.6/);
+  assert.match(DOCUMENTATION_PAGES["release-notes"].html, /3\.2\.7/);
   assert.equal(DOCUMENTATION_PAGES.about.title, "About Us");
   assert.match(DOCUMENTATION_PAGES.about.html, /The LUMOS Team/);
   assert.match(html, /id="homePage"/);
@@ -2530,6 +2534,9 @@ test("public Home and permanent documentation page keep release metadata out of
   assert.match(html, /fonts\.googleapis\.com\/css2\?family=Inter/);
   assert.match(html, /family=Orbitron/);
   assert.match(html, /family=Tektur/);
+  assert.match(siteSource, /mouseenter/);
+  assert.match(siteSource, /mouseleave/);
+  assert.match(siteSource, /\(hover: hover\) and \(pointer: fine\)/);
   assert.match(html, /class="home-install-mark"[^>]*lumos-mark\.svg/);
   assert.match(html, /class="home-brand-hero"/);
   assert.match(html, /class="home-brand-hero-wordmark">LUMOS/);
