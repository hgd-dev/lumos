diff --git a/CHANGELOG.md b/CHANGELOG.md
index b5760bfb8e794182e87e10dbe4b1475de0418ad9..c38f949777cf053892cc302da22c08fdb407a59d 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,15 @@
 # Changelog
 
+## [3.2.6] - 2026-07-29
+
+### Refined
+
+- Removed the three-dimensional film-strip history from the experimental `home-spiral.html` stage to simplify the composition and eliminate an unnecessary source of rendering cost.
+- Further brightened the ambient background, blueprint grid, chromatic glow, construction axes, and triangular core so the spatial scientific geometry reads more clearly on every scene.
+- Reduced motion overhead again by removing the film-orbit renderer, lowering filter intensity on moving elements, tightening pointer follow-up frames, and simplifying particle density while preserving the brighter cosmic atmosphere.
+- Shortened the spacing between scene centers and reduced the pinned travel distance so transitions feel slightly quicker and more connected.
+- Preserved the stable public Home, the separate `home-3d.html` experiment, all workspaces, and all scientific behavior.
+
 ## [3.2.5] - 2026-07-29
 
 ### Refined
diff --git a/CITATION.cff b/CITATION.cff
index 6e2e259ffbd5c79c88a5ddcca7aaa8c2295d42bd..ab200102e261ff3697c31c72fe82fd9caddf4367 100644
--- a/CITATION.cff
+++ b/CITATION.cff
@@ -2,8 +2,8 @@ cff-version: 1.2.0
 message: "If you use LUMOS in research, cite the software and the associated paper or preprint when available."
 title: "LUMOS: Localized Unified Monitoring Optimization System"
 type: software
-version: 3.2.5
-date-released: 2026-07-28
+version: 3.2.6
+date-released: 2026-07-29
 license: MIT
 abstract: "A stable public socially constrained sequential Bayesian framework and browser-based tool for Heat, Air, Soil, and Water monitoring design, validation, intervention evaluation, cross-domain funding, robust multi-round policy analysis, verified deployment, field campaigns, live outcome tracking, commissioning, calibration, maintenance, and replacement planning."
 keywords:
diff --git a/css/home-spiral.css b/css/home-spiral.css
index 92bf99eb3f1f38b8fb47f1c5eff9f7f4975b8595..1b0493955ef03c89fe5c5f7e6a5a007f7dfa5c9b 100644
--- a/css/home-spiral.css
+++ b/css/home-spiral.css
@@ -11,7 +11,6 @@
   --motion-pointer-x: 0;
   --motion-pointer-y: 0;
   --motion-core-rotation: 0deg;
-  --motion-film-rotation: 0deg;
   --motion-core-tilt: 0deg;
   --motion-grid-shift: 0px;
   --motion-chroma-x: 0px;
@@ -31,8 +30,9 @@ body.home-spiral-page::before {
   inset: 0;
   z-index: -2;
   background:
-    radial-gradient(circle at 48% 38%, rgba(var(--motion-accent-rgb), 0.045), transparent 31%),
-    linear-gradient(180deg, #020304, #040706 62%, #07110e);
+    radial-gradient(circle at 48% 38%, rgba(var(--motion-accent-rgb), 0.095), transparent 34%),
+    radial-gradient(circle at 72% 18%, rgba(84, 116, 255, 0.09), transparent 28%),
+    linear-gradient(180deg, #020304, #040706 56%, #07110e);
   pointer-events: none;
 }
 
@@ -91,8 +91,8 @@ body.home-spiral-page::before {
 
 .spiral-scroll-track {
   position: relative;
-  height: 820vh;
-  min-height: 6100px;
+  height: 760vh;
+  min-height: 5480px;
   background: #020304;
 }
 .spiral-sticky-stage {
@@ -105,10 +105,10 @@ body.home-spiral-page::before {
   isolation: isolate;
   opacity: var(--motion-stage-opacity);
   background:
-    radial-gradient(circle at 50% 46%, rgba(var(--motion-accent-rgb), .085), transparent 30%),
-    radial-gradient(circle at 16% 18%, rgba(78, 110, 255, .11), transparent 32%),
-    radial-gradient(circle at 86% 72%, rgba(67, 222, 196, .085), transparent 36%),
-    radial-gradient(circle at 48% 88%, rgba(255,255,255,.028), transparent 28%),
+    radial-gradient(circle at 50% 46%, rgba(var(--motion-accent-rgb), .16), transparent 33%),
+    radial-gradient(circle at 16% 18%, rgba(78, 110, 255, .18), transparent 34%),
+    radial-gradient(circle at 86% 72%, rgba(67, 222, 196, .14), transparent 38%),
+    radial-gradient(circle at 48% 88%, rgba(255,255,255,.045), transparent 32%),
     #020304;
   transition: opacity 90ms linear;
 }
@@ -138,7 +138,7 @@ body.home-spiral-page::before {
     linear-gradient(90deg, rgba(255,255,255,.032) 1px, transparent 1px) 0 0 / 25% 100%,
     linear-gradient(180deg, rgba(255,255,255,.024) 1px, transparent 1px) 0 0 / 100% 25%;
   mask-image: linear-gradient(to bottom, transparent, black 14%, black 86%, transparent);
-  opacity: .58;
+  opacity: .72;
 }
 .motion-background::after {
   content: "";
@@ -155,62 +155,63 @@ body.home-spiral-page::before {
   z-index: 1;
   width: 100%;
   height: 100%;
-  opacity: .84;
-  filter: drop-shadow(0 0 12px rgba(220,240,234,.24));
-  animation: motionParticleDrift 14s ease-in-out infinite alternate;
-  transform: translate3d(calc(var(--motion-pointer-x) * -9px), calc(var(--motion-pointer-y) * -8px), 0) scale(1.02);
+  opacity: .96;
+  filter: none;
+  animation: none;
+  transform: translate3d(calc(var(--motion-pointer-x) * -5px), calc(var(--motion-pointer-y) * -4px), 0) scale(1.01);
 }
 .motion-blueprint-grid {
   position: absolute;
   inset: -12%;
   z-index: 3;
   background-image:
-    linear-gradient(rgba(210, 232, 224, .038) 1px, transparent 1px),
-    linear-gradient(90deg, rgba(210, 232, 224, .038) 1px, transparent 1px),
-    linear-gradient(rgba(var(--motion-accent-rgb), .025) 1px, transparent 1px),
-    linear-gradient(90deg, rgba(var(--motion-accent-rgb), .025) 1px, transparent 1px);
+    linear-gradient(rgba(210, 232, 224, .055) 1px, transparent 1px),
+    linear-gradient(90deg, rgba(210, 232, 224, .055) 1px, transparent 1px),
+    linear-gradient(rgba(var(--motion-accent-rgb), .045) 1px, transparent 1px),
+    linear-gradient(90deg, rgba(var(--motion-accent-rgb), .045) 1px, transparent 1px);
   background-size: 48px 48px, 48px 48px, 180px 180px, 180px 180px;
-  background-position: var(--motion-grid-shift) calc(var(--motion-grid-shift) * -.6);
+  background-position: 0 0;
   transform: perspective(900px) rotateX(62deg) rotateZ(-15deg) translate3d(0, 31%, -180px) scale(1.45);
   transform-origin: 50% 70%;
-  opacity: .76;
+  opacity: .9;
 }
 .motion-chroma {
   position: absolute;
   z-index: 4;
-  width: min(64vw, 920px);
+  width: min(76vw, 1080px);
   aspect-ratio: 1;
   border-radius: 50%;
-  filter: blur(116px);
-  opacity: .16;
+  filter: blur(148px);
+  opacity: .3;
   mix-blend-mode: screen;
-  transform: translate3d(var(--motion-chroma-x), var(--motion-chroma-y), 0);
+  transform: translate3d(0, 0, 0);
 }
 .motion-chroma-a {
   left: 12%;
   top: -22%;
-  background: radial-gradient(circle, rgba(var(--motion-accent-rgb), .58), transparent 64%);
+  background: radial-gradient(circle, rgba(var(--motion-accent-rgb), .82), transparent 66%);
 }
 .motion-chroma-b {
   right: -7%;
   bottom: -35%;
-  background: radial-gradient(circle, rgba(82, 115, 255, .36), transparent 62%);
+  background: radial-gradient(circle, rgba(82, 115, 255, .58), transparent 64%);
 }
 .motion-axis {
   position: absolute;
   z-index: 5;
-  background: linear-gradient(90deg, transparent, rgba(240, 250, 246, .18), rgba(var(--motion-accent-rgb), .16), transparent);
+  background: linear-gradient(90deg, transparent, rgba(240, 250, 246, .34), rgba(var(--motion-accent-rgb), .34), transparent);
+  box-shadow: 0 0 18px rgba(var(--motion-accent-rgb), .14);
   transform-origin: center;
 }
 .motion-axis::after {
   content: "";
   position: absolute;
-  width: 7px;
-  height: 7px;
-  border: 1px solid rgba(var(--motion-accent-rgb), .72);
+  width: 9px;
+  height: 9px;
+  border: 1px solid rgba(var(--motion-accent-rgb), .88);
   border-radius: 50%;
   background: #020304;
-  box-shadow: 0 0 26px rgba(var(--motion-accent-rgb), .52), 0 0 8px rgba(255,255,255,.18);
+  box-shadow: 0 0 36px rgba(var(--motion-accent-rgb), .72), 0 0 12px rgba(255,255,255,.24);
 }
 .motion-axis-horizontal { left: 3%; right: 3%; top: 50%; height: 1px; }
 .motion-axis-horizontal::after { left: 50%; top: -2px; }
@@ -231,10 +232,8 @@ body.home-spiral-page::before {
     rotateX(calc(64deg + var(--motion-pointer-y) * -2deg))
     rotateZ(var(--motion-core-rotation));
   transform-style: preserve-3d;
-  opacity: calc(.38 + var(--motion-gap) * .42);
-  filter:
-    drop-shadow(0 0 28px rgba(var(--motion-accent-rgb), .24))
-    drop-shadow(0 0 92px rgba(var(--motion-accent-rgb), .18));
+  opacity: calc(.62 + var(--motion-gap) * .24);
+  filter: drop-shadow(0 0 34px rgba(var(--motion-accent-rgb), .32));
 }
 .motion-core svg { width: 100%; height: 100%; overflow: visible; }
 .motion-core path,
@@ -243,17 +242,17 @@ body.home-spiral-page::before {
   vector-effect: non-scaling-stroke;
 }
 .core-guides path,
-.core-guides circle { stroke: rgba(236, 246, 242, .28); stroke-width: 1.05; }
+.core-guides circle { stroke: rgba(236, 246, 242, .44); stroke-width: 1.2; }
 .core-signal path,
 .core-signal circle {
-  stroke: rgba(var(--motion-accent-rgb), .92);
-  stroke-width: 1.55;
-  stroke-dasharray: 5 7;
+  stroke: rgba(var(--motion-accent-rgb), .98);
+  stroke-width: 1.9;
+  stroke-dasharray: 6 6;
 }
-.core-nodes { filter: drop-shadow(0 0 10px rgba(var(--motion-accent-rgb), .28)); }
+.core-nodes { filter: drop-shadow(0 0 14px rgba(var(--motion-accent-rgb), .46)) drop-shadow(0 0 32px rgba(var(--motion-accent-rgb), .2)); }
 .core-nodes circle {
   fill: rgba(var(--motion-accent-rgb), .96);
-  stroke: rgba(255,255,255,.86);
+  stroke: rgba(255,255,255,.94);
   stroke-width: .9;
 }
 
@@ -262,6 +261,7 @@ body.home-spiral-page::before {
   inset: 0;
   z-index: 7;
   transform-style: preserve-3d;
+  contain: layout paint style;
 }
 .motion-ghost-orbit span {
   position: absolute;
@@ -276,9 +276,10 @@ body.home-spiral-page::before {
   letter-spacing: .12em;
   line-height: .8;
   opacity: 0;
-  filter: blur(7px);
+  filter: blur(2px);
   transform: translate3d(-50%, -50%, -300px);
-  will-change: transform, opacity, filter;
+  will-change: transform, opacity;
+  contain: layout paint style;
 }
 
 .motion-ghost-label {
@@ -298,136 +299,13 @@ body.home-spiral-page::before {
   text-align: center;
   text-transform: uppercase;
   opacity: 0;
-  filter: blur(4px);
+  filter: blur(1.5px);
   transform: translate3d(-50%, -50%, -180px) rotateZ(-8deg) scale(.92);
-  will-change: transform, opacity, filter;
+  will-change: transform, opacity;
+  contain: layout paint style;
 }
 
-.motion-film-orbit {
-  position: absolute;
-  inset: 0;
-  z-index: 5;
-  overflow: hidden;
-  perspective: 1600px;
-  perspective-origin: 44% 49%;
-  transform-style: preserve-3d;
-  pointer-events: none;
-  mask-image: linear-gradient(90deg, #000 0%, #000 68%, rgba(0,0,0,.84) 82%, transparent 98%);
-}
-.motion-film-track {
-  position: absolute;
-  left: 35%;
-  top: 52%;
-  border-radius: 50%;
-  transform-style: preserve-3d;
-  pointer-events: none;
-}
-.motion-film-track-outer {
-  width: min(82vw, 1160px);
-  aspect-ratio: 1.78;
-  border: 1px solid rgba(var(--motion-accent-rgb), .28);
-  box-shadow:
-    0 0 0 10px rgba(255,255,255,.018),
-    0 0 0 14px rgba(var(--motion-accent-rgb),.075),
-    0 0 120px rgba(var(--motion-accent-rgb), .12);
-  transform: translate3d(-50%, -50%, -330px) rotateX(71deg) rotateZ(calc(-34deg + var(--motion-film-rotation)));
-  opacity: calc(.36 + var(--motion-gap) * .30);
-}
-.motion-film-track-inner {
-  width: min(68vw, 930px);
-  aspect-ratio: 1.74;
-  border: 1px dashed rgba(232,244,239,.18);
-  box-shadow: 0 0 0 5px rgba(0,0,0,.22), 0 0 55px rgba(var(--motion-accent-rgb), .08);
-  transform: translate3d(-50%, -50%, -300px) rotateX(71deg) rotateZ(calc(-34deg + var(--motion-film-rotation)));
-  opacity: calc(.32 + var(--motion-gap) * .24);
-}
-.motion-film-track::before,
-.motion-film-track::after {
-  content: "";
-  position: absolute;
-  inset: -8px;
-  border: 1px dashed rgba(235,246,241,.09);
-  border-radius: inherit;
-}
-.motion-film-track::after {
-  inset: 8px;
-  border-color: rgba(var(--motion-accent-rgb), .08);
-}
-.motion-film-frame {
-  position: absolute;
-  left: 50%;
-  top: 50%;
-  display: grid;
-  grid-template-columns: auto 1fr;
-  grid-template-rows: auto auto;
-  align-content: center;
-  column-gap: 12px;
-  width: clamp(154px, 15.5vw, 238px);
-  aspect-ratio: 1.72;
-  padding: 18px 19px 16px;
-  border: 1px solid rgba(230,242,237,.28);
-  color: rgba(244,249,247,.84);
-  background:
-    linear-gradient(135deg, rgba(var(--motion-accent-rgb), .14), transparent 46%),
-    repeating-linear-gradient(90deg, rgba(255,255,255,.04) 0 1px, transparent 1px 18px),
-    rgba(4,8,8,.84);
-  box-shadow:
-    0 30px 80px rgba(0,0,0,.42),
-    0 0 52px rgba(var(--motion-accent-rgb), .13) inset,
-    0 0 24px rgba(var(--motion-accent-rgb), .08);
-  font-family: var(--font-display);
-  text-transform: uppercase;
-  opacity: 0;
-  contain: paint;
-  backface-visibility: hidden;
-  transform-origin: 50% 50%;
-  will-change: transform, opacity, filter;
-}
-.motion-film-frame::before,
-.motion-film-frame::after {
-  content: "";
-  position: absolute;
-  left: 7px;
-  right: 7px;
-  height: 5px;
-  background: repeating-linear-gradient(90deg, rgba(236,245,241,.34) 0 6px, transparent 6px 13px);
-  opacity: .58;
-}
-.motion-film-frame::before { top: 5px; }
-.motion-film-frame::after { bottom: 5px; }
-.motion-film-frame > span {
-  grid-row: 1 / 3;
-  align-self: center;
-  color: rgba(var(--motion-accent-rgb), .88);
-  font-family: "Orbitron", var(--font-display);
-  font-size: .6rem;
-  letter-spacing: .09em;
-}
-.motion-film-frame strong {
-  align-self: end;
-  font-family: "Orbitron", var(--font-display);
-  font-size: clamp(.78rem, 1.1vw, 1.08rem);
-  letter-spacing: .12em;
-}
-.motion-film-frame small {
-  align-self: start;
-  margin-top: 4px;
-  color: rgba(224,236,231,.58);
-  font-size: .46rem;
-  letter-spacing: .14em;
-}
-.motion-film-frame[data-film-domain="heat"] { --film-tint: 255,154,107; }
-.motion-film-frame[data-film-domain="air"] { --film-tint: 141,231,255; }
-.motion-film-frame[data-film-domain="water"] { --film-tint: 119,170,255; }
-.motion-film-frame[data-film-domain="soil"] { --film-tint: 216,170,114; }
-.motion-film-frame[data-film-domain="identity"] { --film-tint: 189,252,107; }
-.motion-film-frame:not([data-film-domain="unified"]):not([data-film-domain="identity"]) {
-  border-color: rgba(var(--film-tint), .27);
-  background:
-    radial-gradient(circle at 74% 30%, rgba(var(--film-tint), .18), transparent 35%),
-    repeating-linear-gradient(90deg, rgba(255,255,255,.032) 0 1px, transparent 1px 18px),
-    rgba(4,8,8,.86);
-}
+
 
 .motion-micro-ui {
   position: absolute;
@@ -466,13 +344,14 @@ body.home-spiral-page::before {
   display: grid;
   grid-template-columns: minmax(0, .9fr) minmax(360px, 1.1fr);
   align-items: center;
-  gap: clamp(26px, 5vw, 90px);
+  gap: clamp(22px, 4.2vw, 76px);
   padding: clamp(40px, 6vw, 96px) clamp(26px, 7vw, 112px);
   opacity: 0;
   visibility: hidden;
   pointer-events: none;
   transform-style: preserve-3d;
-  will-change: transform, opacity, filter;
+  will-change: transform, opacity;
+  contain: layout paint style;
 }
 .spiral-item.is-active { pointer-events: auto; }
 .motion-scene-copy {
@@ -668,8 +547,9 @@ body.home-spiral-page::before {
   overflow: hidden;
   border: 1px solid rgba(255,255,255,.11);
   background: rgba(5, 8, 9, .78);
-  box-shadow: 0 35px 100px rgba(0,0,0,.44), 0 0 0 1px rgba(var(--motion-accent-rgb), .018) inset;
+  box-shadow: 0 30px 86px rgba(0,0,0,.42), 0 0 0 1px rgba(var(--motion-accent-rgb), .024) inset;
   transform-style: preserve-3d;
+  contain: layout paint style;
 }
 .motion-plane::before {
   content: "";
@@ -936,7 +816,7 @@ body:not(.spiral-handoff-ready) .spiral-after-content { pointer-events:none; }
 }
 
 @media (max-width: 780px) {
-  .spiral-scroll-track { height:750vh;min-height:4700px; }
+  .spiral-scroll-track { height:690vh;min-height:4300px; }
   .home-spiral-header { min-height:56px;padding-inline:16px; }
   .home-spiral-header .header-actions { margin-left:auto; }
   .home-spiral-header #systemCheckButton,.home-spiral-experiment-chip { display:none; }
@@ -956,10 +836,8 @@ body:not(.spiral-handoff-ready) .spiral-after-content { pointer-events:none; }
   .motion-scene-metrics { width:100%;margin-top:18px; }
   .motion-plane-stack { position:absolute;right:-12%;top:11%;width:min(78vw,520px);opacity:.58;transform:translate3d(var(--scene-plane-x,0),var(--scene-plane-y,0),-100px) rotateY(-10deg) rotateX(4deg) scale(.82); }
   .motion-scene-copy { z-index:5;text-shadow:0 12px 30px #020304; }
-  .motion-core { width:94vw;opacity:calc(.11 + var(--motion-gap)*.28); }
-  .motion-film-orbit { mask-image:linear-gradient(90deg,#000 0%,#000 76%,transparent 100%); }
-  .motion-film-frame { width:clamp(118px,30vw,170px);padding:14px 14px 12px; }
-  .motion-blueprint-grid { opacity:.42; }
+  .motion-core { width:94vw;opacity:calc(.28 + var(--motion-gap)*.22); }
+  .motion-blueprint-grid { opacity:.58; }
   .motion-scene-actions { margin-top:15px; }
   .spiral-stage-footer { left:16px;right:16px;bottom:14px;grid-template-columns:1fr auto; }
   .spiral-progress { justify-self:end; }
@@ -984,8 +862,8 @@ body:not(.spiral-handoff-ready) .spiral-after-content { pointer-events:none; }
   .spiral-scroll-track { height:auto;min-height:0; }
   .spiral-sticky-stage { position:relative;height:auto;min-height:0;overflow:visible;padding:90px 20px 70px; }
   .motion-background { position:fixed;opacity:.35; }
-  .motion-core,.motion-blueprint-grid,.motion-ghost-orbit,.motion-film-orbit,.motion-chroma { transform:none!important;animation:none!important; }
-  .motion-ghost-label,.motion-film-orbit { display:none; }
+  .motion-core,.motion-blueprint-grid,.motion-ghost-orbit,.motion-chroma { transform:none!important;animation:none!important; }
+  .motion-ghost-label { display:none; }
   .motion-micro-ui,.spiral-scroll-cue,.spiral-progress { display:none; }
   .spiral-viewport { position:relative;inset:auto;overflow:visible;perspective:none; }
   .spiral-orbit { position:relative;display:grid;gap:80px;transform:none!important; }
diff --git a/docs/PUBLIC_RELEASE.md b/docs/PUBLIC_RELEASE.md
index ec3b3f486a2a879d56827287ce33bca26a3a2a52..3457b3a4a154226da92a3ec0d6879ee156645299 100644
--- a/docs/PUBLIC_RELEASE.md
+++ b/docs/PUBLIC_RELEASE.md
@@ -1,10 +1,12 @@
-# LUMOS 3.2.5 Multi-Page Public Release
+# LUMOS 3.2.6 Multi-Page Public Release
 
-## v3.2.5 lower-lag cosmic refinement and brighter scientific stage
+## v3.2.6 simplified cosmic stage and tighter editorial motion
 
-The unlinked `home-spiral.html` route now runs with lower residual motion lag while keeping the same six-scene editorial structure, stable public Home, and scientific workspaces. Per-frame blur intensity has been reduced, pointer-settling completes sooner, and the particle canvas uses a slightly lighter raster budget so the stage responds more quickly.
+The unlinked `home-spiral.html` route no longer includes the three-dimensional film-strip history. Removing that background system simplifies the composition and eliminates its per-frame transform, opacity, filter, and depth calculations.
 
-At the same time, the persistent cosmic environment has been increased again. The background now uses a denser starfield, stronger nebula glow, a brighter blueprint field, and more visible ambient chroma. The orbital film ribbon, its rails, and its interior frame glow have been brightened so the strip remains legible behind the sequence. The axial triangle, guide lines, signal strokes, and node glow are also more visible without changing the underlying scientific content.
+The persistent environment is stronger instead: brighter construction axes, a more visible triangular core, clearer guide and signal lines, stronger node glow, a brighter blueprint grid, and larger scene-reactive chromatic fields. Particle count is reduced while the static nebula treatment is stronger, producing a fuller space-and-technology atmosphere at lower runtime cost.
+
+Scene centers and the total pinned scroll distance are slightly closer together, so transitions feel more connected while retaining a foreground-free ambient gap.
 
 No scientific model, objective, constraint, evidence generator, stable public page, or workspace behavior changed.
 
@@ -87,7 +89,7 @@ See `docs/COMMISSIONING_AND_MAINTENANCE.md`, `docs/LIMITATIONS.md`, and `docs/PR
 
 ## Brand and Home refinement
 
-The 3.2.5 public architecture uses separate static entry pages for Home, About Us, Documentation, Research & Process, Contact & Feedback, Unified, Heat, Air, Soil, and Water. A shared workspace shell and shared JavaScript model preserve one scientific implementation across every workspace URL, while lightweight informational pages keep public navigation clear. New sessions open at the full United States extent, while saved workspaces are restored only by explicit user action.
+The 3.2.6 public architecture uses separate static entry pages for Home, About Us, Documentation, Research & Process, Contact & Feedback, Unified, Heat, Air, Soil, and Water. A shared workspace shell and shared JavaScript model preserve one scientific implementation across every workspace URL, while lightweight informational pages keep public navigation clear. New sessions open at the full United States extent, while saved workspaces are restored only by explicit user action.
 
 
 ## v3.1.4 information architecture
diff --git a/docs/RELEASE_CHECKLIST.md b/docs/RELEASE_CHECKLIST.md
index 842497af89176bff99792977c091f0eb10dd5e48..393997cb382df9d4dc15713e2f41a7ab28756180 100644
--- a/docs/RELEASE_CHECKLIST.md
+++ b/docs/RELEASE_CHECKLIST.md
@@ -2,9 +2,9 @@
 
 ## Version and package
 
-- [ ] `package.json`, `release.json`, `js/release/version.js`, service worker, citation metadata, release notes, and changelog identify `3.2.5`. The public masthead, browser title, footer, manifest name, README title, and model-specification title remain version-neutral.
+- [ ] `package.json`, `release.json`, `js/release/version.js`, service worker, citation metadata, release notes, and changelog identify `3.2.6`. The public masthead, browser title, footer, manifest name, README title, and model-specification title remain version-neutral.
 - [ ] Release status is `stable-public-v3`.
-- [ ] Service-worker cache is `lumos-v3.2.5` and contains every standard public entry page, the unlinked `home-3d.html` and `home-spiral.html` experiments, the shared workspace shell, all public domain modules, unified operations, and documentation scripts.
+- [ ] Service-worker cache is `lumos-v3.2.6` and contains every standard public entry page, the unlinked `home-3d.html` and `home-spiral.html` experiments, the shared workspace shell, all public domain modules, unified operations, and documentation scripts.
 - [ ] No permanent credential is embedded in public assets.
 
 ## Automated verification
diff --git a/home-spiral.html b/home-spiral.html
index 68dada6851d815eaf1aa54177ce7d84a7c9f2475..bc1a2c7912ec22db131deb305a4dd22bf6020c38 100644
--- a/home-spiral.html
+++ b/home-spiral.html
@@ -93,16 +93,7 @@
               </g>
             </svg>
           </div>
-          <div id="motionFilmOrbit" class="motion-film-orbit" aria-hidden="true">
-            <div class="motion-film-track motion-film-track-outer"></div>
-            <div class="motion-film-track motion-film-track-inner"></div>
-            <div class="motion-film-frame" data-film-index="0" data-film-domain="identity"><span>00</span><strong>LUMOS</strong><small>Design identity</small></div>
-            <div class="motion-film-frame" data-film-index="1" data-film-domain="unified"><span>01</span><strong>UNIFIED</strong><small>Shared system</small></div>
-            <div class="motion-film-frame" data-film-index="2" data-film-domain="heat"><span>02</span><strong>HEAT</strong><small>Thermal field</small></div>
-            <div class="motion-film-frame" data-film-index="3" data-film-domain="air"><span>03</span><strong>AIR</strong><small>Transport field</small></div>
-            <div class="motion-film-frame" data-film-index="4" data-film-domain="water"><span>04</span><strong>WATER</strong><small>Flow field</small></div>
-            <div class="motion-film-frame" data-film-index="5" data-film-domain="soil"><span>05</span><strong>SOIL</strong><small>Depth field</small></div>
-          </div>
+
         </div>
 
         <div class="motion-micro-ui motion-micro-left" aria-hidden="true">
diff --git a/js/home-spiral.js b/js/home-spiral.js
index 8ce92a70d54311304fda08d1698e31cc6edb8785..d238a054e20f434876fde3287a01ceeefe1d42f5 100644
--- a/js/home-spiral.js
+++ b/js/home-spiral.js
@@ -4,7 +4,6 @@ const orbit = document.querySelector("#spiralOrbit");
 const cards = Array.from(document.querySelectorAll(".spiral-item"));
 const ghostItems = Array.from(document.querySelectorAll("#motionGhostOrbit > span"));
 const ghostLabel = document.querySelector("#motionGhostLabel");
-const filmFrames = Array.from(document.querySelectorAll(".motion-film-frame"));
 const typingWord = document.querySelector("#motionTypingWord");
 const particleCanvas = document.querySelector("#spiralParticleCanvas");
 const counter = document.querySelector("#spiralCounter");
@@ -17,12 +16,12 @@ const rootStyle = document.documentElement.style;
 const reducedMotionQuery = window.matchMedia?.("(prefers-reduced-motion: reduce)");
 const coarsePointerQuery = window.matchMedia?.("(pointer: coarse)");
 
-const SCENE_CENTERS = [0, 0.17, 0.34, 0.51, 0.68, 0.85];
-const SCENE_INNER_RADIUS = 0.02;
-const SCENE_OUTER_RADIUS = 0.057;
-const SCENE_INTERVAL = 0.17;
-const SNAP_RADIUS = 0.033;
-const SNAP_DELAY = 170;
+const SCENE_CENTERS = [0, 0.16, 0.32, 0.48, 0.64, 0.8];
+const SCENE_INNER_RADIUS = 0.019;
+const SCENE_OUTER_RADIUS = 0.051;
+const SCENE_INTERVAL = 0.16;
+const SNAP_RADIUS = 0.03;
+const SNAP_DELAY = 145;
 const STAGE_BLACK_START = 0.92;
 const STAGE_BLACK_END = 0.945;
 const STAGE_FADE_START = 0.95;
@@ -149,53 +148,18 @@ function renderGhostOrbit(progress, gapIntensity) {
     const z = -260 - absolute * 300;
     const proximity = clamp(1 - absolute * 0.5, 0, 1);
     const opacity = (0.012 + gapIntensity * 0.17) * proximity;
-    const blur = 3 + absolute * 3.4 + (1 - gapIntensity) * 1.1;
     ghost.style.opacity = opacity.toFixed(3);
-    ghost.style.filter = `blur(${blur.toFixed(2)}px)`;
     ghost.style.visibility = opacity > 0.004 ? "visible" : "hidden";
     ghost.style.transform = `translate3d(calc(-50% + ${x.toFixed(1)}px), calc(-50% + ${y.toFixed(1)}px), ${z.toFixed(1)}px) rotateY(${(offset * -28).toFixed(2)}deg) rotateZ(${(angle * 12).toFixed(2)}deg)`;
   });
 }
 
-function renderFilmOrbit(progress, gapIntensity) {
-  if (!filmFrames.length) return;
-  const width = Math.max(1, window.innerWidth);
-  const height = Math.max(1, window.innerHeight);
-  const radiusX = Math.min(width * 0.39, 590);
-  const radiusY = Math.min(height * 0.24, 220);
-  const radiusZ = Math.min(width * 0.25, 380);
-  const centerShiftX = -Math.min(width * 0.16, 245);
-  const handoffFade = 1 - smoothstep((progress - 0.905) / 0.038);
-
-  filmFrames.forEach((frame, index) => {
-    const center = sceneCenter(index);
-    const reveal = smoothstep((progress - center - 0.026) / 0.028);
-    const angle = progress * 5.4 + index * 0.78 + 2.28;
-    const depthWave = Math.cos(angle);
-    const depth = (depthWave + 1) / 2;
-    const x = centerShiftX + Math.sin(angle) * radiusX;
-    const y = Math.sin(angle * 1.55) * radiusY * 0.38 + depthWave * radiusY * 0.22;
-    const z = -520 + depth * radiusZ;
-    const opacity = reveal * handoffFade * (0.11 + depth * 0.27) * (0.9 + gapIntensity * 0.3);
-    const blur = 1.5 + (1 - depth) * 4.4;
-    const scale = 0.62 + depth * 0.24;
-    const rotateX = 7 - depthWave * 9;
-    const rotateY = Math.sin(angle) * -54;
-    const rotateZ = -24 + Math.sin(angle) * 13;
-
-    frame.style.opacity = opacity.toFixed(3);
-    frame.style.filter = `blur(${blur.toFixed(2)}px)`;
-    frame.style.transform = `translate3d(calc(-50% + ${x.toFixed(1)}px), calc(-50% + ${y.toFixed(1)}px), ${z.toFixed(1)}px) rotateX(${rotateX.toFixed(2)}deg) rotateY(${rotateY.toFixed(2)}deg) rotateZ(${rotateZ.toFixed(2)}deg) scale(${scale.toFixed(3)})`;
-    frame.style.visibility = opacity > 0.008 ? "visible" : "hidden";
-  });
-}
-
 function renderMotion(force = false) {
   frameRequested = false;
   if (reduced || !track || !viewport || !orbit) return;
 
   const progress = scrollProgress();
-  const pointerEase = force ? 1 : 0.26;
+  const pointerEase = force ? 1 : 0.34;
   pointerX = mix(pointerX, targetPointerX, pointerEase);
   pointerY = mix(pointerY, targetPointerY, pointerEase);
   rootStyle.setProperty("--motion-pointer-x", pointerX.toFixed(4));
@@ -221,12 +185,10 @@ function renderMotion(force = false) {
     const rotateY = travel * 9.5;
     const rotateZ = travel * -7.5;
     const scale = clamp(1 - absoluteNormalized * 0.12, 0.76, 1);
-    const blur = smoothstep((Math.abs(distance) - SCENE_INNER_RADIUS) / (SCENE_OUTER_RADIUS - SCENE_INNER_RADIUS)) * 10.5;
-    const visible = opacity > 0.002 && Math.abs(distance) < SCENE_OUTER_RADIUS + 0.02;
+    const visible = opacity > 0.002 && Math.abs(distance) < SCENE_OUTER_RADIUS + 0.016;
 
     card.style.transform = `translate3d(${x.toFixed(1)}px, ${y.toFixed(1)}px, ${z.toFixed(1)}px) rotateX(${rotateX.toFixed(2)}deg) rotateY(${rotateY.toFixed(2)}deg) rotateZ(${rotateZ.toFixed(2)}deg) scale(${scale.toFixed(4)})`;
     card.style.opacity = opacity.toFixed(3);
-    card.style.filter = visible ? `blur(${blur.toFixed(2)}px)` : "blur(0px)";
     card.style.visibility = visible ? "visible" : "hidden";
     card.style.zIndex = String(50 - Math.round(absoluteNormalized * 10));
     card.style.setProperty("--scene-copy-x", `${(travel * width * 0.025).toFixed(1)}px`);
@@ -239,7 +201,6 @@ function renderMotion(force = false) {
   const { index: nearest } = nearestScene(progress);
   setActiveCard(nearest);
   renderGhostOrbit(progress, gapIntensity);
-  renderFilmOrbit(progress, gapIntensity);
   if (ghostLabel) {
     let leftIndex = nearest;
     let rightIndex = nearest;
@@ -254,7 +215,6 @@ function renderMotion(force = false) {
       ? (cards[nearest]?.dataset.spiralTitle || "LUMOS")
       : `${cards[leftIndex]?.dataset.spiralTitle || "LUMOS"} / ${cards[rightIndex]?.dataset.spiralTitle || "LUMOS"}`;
     ghostLabel.style.opacity = (gapIntensity * 0.28).toFixed(3);
-    ghostLabel.style.filter = `blur(${(1.8 + (1 - gapIntensity) * 3.1).toFixed(2)}px)`;
     ghostLabel.style.transform = `translate3d(-50%, -50%, -180px) rotateZ(${(-8 + progress * 13).toFixed(2)}deg) scale(${(0.9 + gapIntensity * 0.08).toFixed(3)})`;
   }
 
@@ -273,10 +233,6 @@ function renderMotion(force = false) {
   rootStyle.setProperty("--motion-content-shift", `${((1 - contentReveal) * 80).toFixed(1)}px`);
   rootStyle.setProperty("--motion-header-opacity", headerOpacity.toFixed(4));
   rootStyle.setProperty("--motion-core-rotation", `${(progress * 390 - 18).toFixed(2)}deg`);
-  rootStyle.setProperty("--motion-film-rotation", `${(progress * 31).toFixed(2)}deg`);
-  rootStyle.setProperty("--motion-grid-shift", `${(progress * 265).toFixed(1)}px`);
-  rootStyle.setProperty("--motion-chroma-x", `${(Math.sin(progress * Math.PI * 2) * 68).toFixed(1)}px`);
-  rootStyle.setProperty("--motion-chroma-y", `${(Math.cos(progress * Math.PI * 1.6) * 42).toFixed(1)}px`);
 
   document.body.classList.toggle("spiral-between-scenes", gapIntensity > 0.48 && progress < STAGE_BLACK_START);
   document.body.classList.toggle("spiral-handoff-active", progress >= STAGE_BLACK_START);
@@ -284,8 +240,8 @@ function renderMotion(force = false) {
 
   lastProgress = progress;
   if (
-    Math.abs(pointerX - targetPointerX) > 0.006 ||
-    Math.abs(pointerY - targetPointerY) > 0.006
+    Math.abs(pointerX - targetPointerX) > 0.012 ||
+    Math.abs(pointerY - targetPointerY) > 0.012
   ) requestFrame();
 }
 
@@ -347,7 +303,7 @@ function runTypingStep() {
       typingPhase = "type";
       scheduleTyping(230);
     } else {
-      scheduleTyping(48);
+      scheduleTyping(42);
     }
     return;
   }
@@ -359,7 +315,7 @@ function runTypingStep() {
     typingPhase = "hold";
     scheduleTyping(80);
   } else {
-    scheduleTyping(74);
+    scheduleTyping(66);
   }
 }
 
@@ -393,7 +349,7 @@ function drawParticleField() {
   context.clearRect(0, 0, width, height);
 
   const random = seededRandom(0x4c554d4f);
-  const count = width < 760 ? 132 : 252;
+  const count = width < 760 ? 108 : 196;
   for (let index = 0; index < count; index += 1) {
     const x = random() * width;
     const y = random() * height;
@@ -418,10 +374,10 @@ function drawParticleField() {
   }
 
   const nebulae = [
-    [width * .18, height * .22, Math.min(width, height) * .22, "rgba(86,116,255,.085)"],
-    [width * .76, height * .34, Math.min(width, height) * .19, "rgba(189,252,107,.06)"],
-    [width * .66, height * .78, Math.min(width, height) * .24, "rgba(66,214,190,.06)"],
-    [width * .42, height * .62, Math.min(width, height) * .18, "rgba(255,255,255,.025)"]
+    [width * .18, height * .22, Math.min(width, height) * .26, "rgba(86,116,255,.12)"],
+    [width * .76, height * .34, Math.min(width, height) * .22, "rgba(189,252,107,.09)"],
+    [width * .66, height * .78, Math.min(width, height) * .28, "rgba(66,214,190,.09)"],
+    [width * .42, height * .62, Math.min(width, height) * .2, "rgba(255,255,255,.04)"]
   ];
   nebulae.forEach(([x, y, radius, color]) => {
     const gradient = context.createRadialGradient(x, y, 0, x, y, radius);
@@ -455,19 +411,11 @@ function clearMotionStyles() {
   ghostItems.forEach((ghost) => {
     ghost.style.removeProperty("transform");
     ghost.style.removeProperty("opacity");
-    ghost.style.removeProperty("filter");
     ghost.style.removeProperty("visibility");
   });
-  filmFrames.forEach((frame) => {
-    frame.style.removeProperty("transform");
-    frame.style.removeProperty("opacity");
-    frame.style.removeProperty("filter");
-    frame.style.removeProperty("visibility");
-  });
   if (ghostLabel) {
     ghostLabel.style.removeProperty("transform");
     ghostLabel.style.removeProperty("opacity");
-    ghostLabel.style.removeProperty("filter");
   }
 }
 
diff --git a/js/release/documentation.js b/js/release/documentation.js
index 36a17c3057943871804287961172ebc64c3e6ca8..6f44af7f0b453e2b54b6cf29f4c321184a80bb91 100644
--- a/js/release/documentation.js
+++ b/js/release/documentation.js
@@ -116,16 +116,16 @@ export const DOCUMENTATION_PAGES = Object.freeze({
   }),
   "release-notes": Object.freeze({
     title: "Release notes",
-    kicker: "Current public release: 3.2.5",
-    summary: "Lower lag, a bigger cosmic background, a brighter ribbon strip, and a more visible axial system core.",
+    kicker: "Current public release: 3.2.6",
+    summary: "No film strip, stronger ambient geometry, lower runtime overhead, and tighter scene spacing.",
     html: `
-      <p><strong>LUMOS 3.2.5</strong> further refines the unlinked <code>home-spiral.html</code> experiment with lower motion lag and a stronger cosmic technical backdrop while preserving the stable Home and every scientific workspace.</p>
+      <p><strong>LUMOS 3.2.6</strong> further refines the unlinked <code>home-spiral.html</code> experiment by removing the film-strip history, strengthening the luminous geometric background, reducing motion overhead, and tightening scene spacing.</p>
       <h3>Experimental presentation</h3>
       <ul>
-        <li>Reduced residual input lag by lowering per-frame blur work, shortening pointer settling time, and trimming particle-canvas raster cost.</li>
-        <li>Expanded the background with a denser starfield, stronger nebula blooms, and a brighter blueprint field.</li>
-        <li>Brightened the orbital film ribbon, its rails, and its interior glow so the strip remains visible behind the editorial sequence.</li>
-        <li>Made the axial triangle, guide lines, signal strokes, and node glow easier to read.</li>
+        <li>Removed the three-dimensional film-strip history and its per-frame renderer.</li>
+        <li>Brightened the starfield, chromatic glow, blueprint grid, axes, triangular core, guide lines, signal strokes, and nodes.</li>
+        <li>Reduced runtime filter work, pointer follow-up frames, and particle cost while preserving a stronger cosmic atmosphere.</li>
+        <li>Shortened scene-center spacing and the pinned scroll distance for a more connected sequence.</li>
         <li>Kept the stable Home and every scientific workspace unchanged.</li>
       </ul>
       <h3>Scientific scope</h3>
diff --git a/js/release/public-readiness.js b/js/release/public-readiness.js
index 51517f244335b6d7efffbe38d709b04c497410f1..fc08aa6d151c3948b2eeb2338b3cd4d3bd217fe3 100644
--- a/js/release/public-readiness.js
+++ b/js/release/public-readiness.js
@@ -93,7 +93,7 @@ export function runPublicLaunchReadiness(options = {}) {
 
   checks.push(readinessCheck({
     id: "release-version", category: "release", label: "Release metadata identity",
-    satisfied: release.version === "3.2.5",
+    satisfied: release.version === "3.2.6",
     detail: release.version ? "Release metadata, citation, and packaged assets identify one consistent release." : "Release metadata does not identify a packaged release."
   }));
   checks.push(readinessCheck({
diff --git a/js/release/version.js b/js/release/version.js
index 1554f8c76ed75aa5062c1fe96942621ba33ef3fb..ff2416aac6bea08ea9e501df05a17ed82251a89b 100644
--- a/js/release/version.js
+++ b/js/release/version.js
@@ -1,4 +1,4 @@
 export const APP_NAME = "LUMOS";
-export const APP_VERSION = "3.2.5";
+export const APP_VERSION = "3.2.6";
 export const RELEASE_CHANNEL = "stable-public";
-export const RELEASE_DATE = "2026-07-28";
+export const RELEASE_DATE = "2026-07-29";
diff --git a/package.json b/package.json
index a10d4b4e6776661115ff9b485cfeafb6484a6a7e..5433a156eb69b5a206b1e0e7b285a818c3093b29 100644
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "lumos-core",
-  "version": "3.2.5",
+  "version": "3.2.6",
   "private": true,
   "type": "module",
   "scripts": {
diff --git a/release.json b/release.json
index a06bcc0a612e1fd62e5b43221272e2b0b40d038e..4518871c335b7af2ddff76adc13924edff2a7ad7 100644
--- a/release.json
+++ b/release.json
@@ -1,6 +1,6 @@
 {
   "name": "LUMOS",
-  "version": "3.2.5",
+  "version": "3.2.6",
   "releaseDate": "2026-07-29",
   "status": "stable-public-v3",
   "primaryDomain": "multi-domain",
@@ -130,7 +130,7 @@
       "entry": "home-spiral.html",
       "linkedFromPrimaryNavigation": false,
       "indexing": "noindex-nofollow",
-      "renderer": "optimized-native-css-editorial-motion-canvas-with-split-identity-and-unified-scenes-custom-domain-glyphs-and-left-wrapping-three-dimensional-film-ribbon-history",
+      "renderer": "optimized-native-css-editorial-motion-canvas-with-split-identity-and-unified-scenes-custom-domain-glyphs-and-brightened-ambient-geometric-background",
       "interaction": [
         "sticky-pinned-continuous-motion-stage",
         "first-frame-lumos-hero-without-empty-lead-in",
@@ -138,7 +138,6 @@
         "six-scene-editorial-typography-system-with-separated-unified-overview",
         "custom-technical-heat-air-water-and-soil-glyphs",
         "persistent-blueprint-grid-and-wireframe-core",
-        "completed-scene-film-frames-wrapping-left-and-behind-central-geometry",
         "depth-separated-domain-visual-planes",
         "steep-diagonal-inertial-scene-transitions",
         "foreground-free-ambient-gaps-between-scenes",
diff --git a/scripts/release-check.mjs b/scripts/release-check.mjs
index eea7a23f9003507599543d235812c5cddb8e5d1d..59ea81fbe0f5cf4a8b326dfdef848eb92df645f3 100644
--- a/scripts/release-check.mjs
+++ b/scripts/release-check.mjs
@@ -5,7 +5,7 @@ import { fileURLToPath } from "node:url";
 import { spawnSync } from "node:child_process";
 
 const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
-const expectedVersion = "3.2.5";
+const expectedVersion = "3.2.6";
 const required = [
   "index.html", "home-3d.html", "home-spiral.html", "about.html", "documentation.html", "research.html", "contact.html", "unified.html", "heat.html", "air.html", "soil.html", "water.html", "workspace-shell.html",
   "package.json", "release.json", "manifest.webmanifest", "service-worker.js", "404.html",
diff --git a/service-worker.js b/service-worker.js
index 3d6bbd657f702c72ae0897917236ffd52f38c0db..6ac36f89167c0324eaea2c49267adb38f1747c54 100644
--- a/service-worker.js
+++ b/service-worker.js
@@ -1,4 +1,4 @@
-const CACHE_NAME = "lumos-v3.2.5";
+const CACHE_NAME = "lumos-v3.2.6";
 const LIBRARY_ASSETS = [
   "https://unpkg.com/maplibre-gl@5.6.0/dist/maplibre-gl.css",
   "https://unpkg.com/maplibre-gl@5.6.0/dist/maplibre-gl.js",
diff --git a/tests/model.test.js b/tests/model.test.js
index d5b166dc00bebc469841b24d179bbb14b7f2c00a..182ee9e35e7ee8c057337c9bde9301974d3ca971 100644
--- a/tests/model.test.js
+++ b/tests/model.test.js
@@ -2170,7 +2170,7 @@ test("multi-page Unified interface exposes commissioning and full-map controls",
 
 test("cross-domain consistency audit passes and exports reproducible tidy rows", () => {
   const releaseMetadata = {
-    version: "3.2.5",
+    version: "3.2.6",
     status: "stable-public-v3",
     supportedDomains: ["heat", "air", "soil", "water"],
     supportedWorkspaces: [
@@ -2365,9 +2365,7 @@ test("secret motion Home uses a continuous editorial stage and remains unlinked"
   assert.match(spiral, /class="motion-core"/);
   assert.match(spiral, /class="motion-blueprint-grid"/);
   assert.match(spiral, /id="motionTypingWord"/);
-  assert.match(spiral, /class="motion-film-orbit"/);
-  assert.match(spiral, /motion-film-track motion-film-track-outer/);
-  assert.match(spiral, /motion-film-track motion-film-track-inner/);
+  assert.doesNotMatch(spiral, /class="motion-film-orbit"/);
   assert.match(spiral, /data-spiral-title="UNIFIED"/);
   assert.match(spiral, /id="spiralCounter">01 \/ 06/);
   assert.match(spiral, /class="domain-glyph"/);
@@ -2388,13 +2386,13 @@ test("secret motion Home uses a continuous editorial stage and remains unlinked"
   assert.match(styles, /motion-ghost-orbit/);
   assert.match(styles, /margin-top:\s*-100vh/);
   assert.match(script, /scrollProgress\(\)/);
-  assert.match(script, /const SCENE_CENTERS = \[0, 0\.17, 0\.34, 0\.51, 0\.68, 0\.85\]/);
+  assert.match(script, /const SCENE_CENTERS = \[0, 0\.16, 0\.32, 0\.48, 0\.64, 0\.8\]/);
   assert.match(script, /CONTENT_REVEAL_START/);
   assert.match(script, /TYPING_WORDS/);
   assert.match(script, /renderGhostOrbit/);
-  assert.match(script, /renderFilmOrbit/);
-  assert.match(script, /const SNAP_RADIUS = 0\.033/);
-  assert.match(script, /card\.style\.filter/);
+  assert.doesNotMatch(script, /renderFilmOrbit/);
+  assert.match(script, /const SNAP_RADIUS = 0\.03/);
+  assert.doesNotMatch(script, /card\.style\.filter/);
   assert.match(script, /drawParticleField/);
   assert.match(script, /spiral-reduced/);
 });
@@ -2405,7 +2403,7 @@ test("v3 release metadata is internally consistent", async () => {
   const release = JSON.parse(await readFile(new URL("../release.json", import.meta.url), "utf8"));
   const manifest = JSON.parse(await readFile(new URL("../manifest.webmanifest", import.meta.url), "utf8"));
   assert.equal(APP_NAME, "LUMOS");
-  assert.equal(APP_VERSION, "3.2.5");
+  assert.equal(APP_VERSION, "3.2.6");
   assert.equal(RELEASE_CHANNEL, "stable-public");
   assert.equal(packageJson.version, APP_VERSION);
   assert.equal(packageJson.scripts.verify, "node scripts/run-verification.mjs");
@@ -2448,7 +2446,7 @@ test("v3 release metadata is internally consistent", async () => {
 test("v3 service worker preserves the complete same-origin application shell", async () => {
   const { readFile } = await import("node:fs/promises");
   const worker = await readFile(new URL("../service-worker.js", import.meta.url), "utf8");
-  assert.match(worker, /lumos-v3\.2\.5/);
+  assert.match(worker, /lumos-v3\.2\.6/);
   assert.match(worker, /\.\/index\.html/);
   assert.match(worker, /\.\/home-3d\.html/);
   assert.match(worker, /\.\/css\/home-3d\.css/);
@@ -2517,7 +2515,7 @@ test("public Home and permanent documentation page keep release metadata out of
   assert.equal(DEFAULT_DOCUMENTATION_PAGE, "quickstart");
   assert.equal(DOCUMENTATION_ORDER.length, 8);
   assert.ok(DOCUMENTATION_ORDER.every((key) => DOCUMENTATION_PAGES[key]?.title && DOCUMENTATION_PAGES[key]?.html));
-  assert.match(DOCUMENTATION_PAGES["release-notes"].html, /3\.2\.5/);
+  assert.match(DOCUMENTATION_PAGES["release-notes"].html, /3\.2\.6/);
   assert.equal(DOCUMENTATION_PAGES.about.title, "About Us");
   assert.match(DOCUMENTATION_PAGES.about.html, /The LUMOS Team/);
   assert.match(html, /id="homePage"/);
